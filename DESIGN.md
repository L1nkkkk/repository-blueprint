# 仓库代码梳理工具：设计基线

更新日期：2026-09-12

本文记录当前讨论中已采纳的产品与交互设计，作为后续原型、实现方案和评审的依据。

Skill 的交付内容、阅读流程、图谱对象、分析状态和完成标准，已细化为 [Skill 工作规范草案](SKILL_SPEC.md)。该草案用于下一轮评审与真实代码样本验证。

从启动梳理、阶段性浏览、节点提问到暂停续接和源码更新的操作约定见 [完整使用流程](USER_FLOW.md)；组件交付、实现顺序与验收要求见 [首版交付规格](V1_SCOPE.md)。现有实现已连接真实仓库扫描、命令式阅读提交与本地画布，固定源码样本已核对；自动 AI 执行和源码更新合并仍待接入。

节点种类与外观见 [节点视觉规范](NODE_DESIGN.md)，可运行的基础格式和批次规则见 [图谱格式](GRAPH_FORMAT.md) 与 [任务协议](TASK_PROTOCOL.md)。

## 产品目标

- 全仓库梳理作为默认主功能：AI 自动遍历仓库、逐模块阅读并整理成统一的仓库地图。
- 理解陌生、复杂仓库的架构、模块职责和处理流程。
- 追踪具体数据的来源、传递、转换、读写和最终去向。
- 目标语言包括 C++、Python、C#、TypeScript 和 JavaScript。
- 以 UE 引擎源码等大型复杂仓库作为重要应用场景。
- 界面交互参考 UE 蓝图：无限画布、节点、输入输出端口和有方向的连线。

## 用户明确的使用方式

用户希望先让 AI 阅读仓库，拆解代码和流程并整理结果，再通过蓝图式画布阅读这些成果。后续实现以这个 AI 主导的梳理工作流为出发点。

用户可以直接要求“梳理整个仓库”，由 AI 自动发现入口、处理模块队列并汇总全局成果。局部追踪和继续拆解节点作为辅助模式，在已有全仓库图谱上开展深入探索。

全仓库分析采用分批推进、保存中间成果和断点恢复。分批控制工作量与阅读节奏，任务范围始终保留为全仓库；用户无需逐个点击节点才能安排后续分析。

首版流程暂按“现有 AI 会话发起 + 配套本地画布阅读器”具体化，任务管理组件负责持久保存，实际执行器负责推进分析。具体 AI 宿主与适配接口需要验证；独立桌面外壳、编辑器插件仍是后续承载形式的候选。

## 当前讨论中的组件分工

以下是基于上述使用方式提出的实现方向，具体接口和交付形式尚未定案：

| 组件 | 建议职责 |
| --- | --- |
| 仓库梳理 Skill | 指导 AI 开展全仓库梳理或局部追踪，逐模块阅读、整理职责与数据关系、记录源码依据 |
| 全仓库任务与进度管理 | 建立覆盖清单和模块队列，管理跨模块核对、分批推进、中间成果与断点恢复 |
| 结构化图谱 | 保存节点、端口、关系、层级、数据流标识、证据和分析进度，供 AI 与画布共同使用 |
| 蓝图画布工具 | 展示与导航图谱，保存用户布局，并为围绕节点继续提问提供入口 |
| 阅读与分析辅助工具 | 提供文件读取、搜索、符号查询，以及按需接入的语言分析能力 |

首版交付建议包含 Skill、本地辅助工具、图谱工程、画布阅读器和至少一个已验证的 AI 执行连接。需要更紧密的工具交互时，再评估 MCP 等接口与插件打包方式。没有执行连接时可阅读已有成果并复制分析请求；只有真实接收后才显示任务已开始。

建议让图谱内容与用户布局分别保存；AI 更新分析内容时，通过稳定标识尽量保持现有节点位置、任务颜色和用户注释。

建议区分“展开已经分析的内容”和“请求 AI 继续分析”。未阅读、部分分析及已有源码依据的内容，应具有明确的状态。画布请求 AI 继续工作的交互，需要与具体 AI 宿主集成。

## 基础交互

- 支持浅色、深色和跟随系统三种主题；切换时保留阅读位置、数据聚焦和分析进度，正式阅读器保存用户偏好。
- 支持全仓库梳理的启动、暂停、继续和进度查看，并可在分析过程中浏览阶段性成果。
- 画布可以自由平移、缩放，节点可以拖动整理布局。
- 支持从入口、模块、事件、函数或具体数据开始探索。
- 节点支持局部展开，周围未展开的部分保留上下文。
- 节点及连线与对应源码位置关联。
- 保存用户整理的阅读布局和阅读位置。

## 节点与层级

文件位置与代码语义提供相互关联的浏览方式：

| 组织方式 | 内容 |
| --- | --- |
| 文件位置 | 仓库、文件夹、文件 |
| 代码语义 | 包、模块、namespace、类、结构体、函数等 |
| 架构职责 | 子系统、模块、插件及其关系 |

具体层级按照语言和项目组织方式适配。函数可直接属于模块；字段、参数和变量按照实际归属展示。

节点通过标题种类色、图标、明确标签和结构区分目录、文件、作用域、类型、函数、数据、运算与外部接口。入口、测试、工具等作为附加角色，分析进度与依据时效另行显示；连线颜色绑定所属任务，具体数据通过名称与版本区分。

追踪数据时支持深入参数、字段、变量和对象状态。文件、类等折叠节点保留跨越其边界的数据端口，并能定位到内部对应位置。

## 数据流配色

- 每个有明确目标的任务或流程具有独立的颜色，图例紧凑显示色点和任务名称，用途在悬停提示与详情中查看。
- 同一任务的多个输入、分支、派生结果和子调用共用任务颜色。
- 计算产生的新数据保留独立身份、版本及输入来源，通过端口名称和详情区分。
- 展开、折叠和重新打开阅读视图时，已有任务的颜色保持稳定。
- 点击任务图例或连线高亮任务；点击端口或输入可进一步追踪单份数据。
- 支持同时选择多个任务进行对照，共用节点保留多个任务色标。

## 已采纳的可读性设计

### 分层展示与缩放

- 缩远时显示节点名称、职责和主要连接。
- 放大后逐渐展示端口、字段和类型。
- 进入文件、类或函数使用明确的展开操作；缩放调整当前层级的信息密度。
- 小节点可原地展开，大模块可进入子画布。
- 通过路径栏返回上层，恢复原来的阅读上下文。

### 固定的节点阅读格式

- 节点默认展示名称、节点类型、一句话职责和输入输出端口。
- 输入端口放在左侧，输出端口放在右侧。
- 完整路径、源码和详细解释放在选中后的详情区域。
- 复杂对象的端口支持分组，例如先显示 Transform，再展开 Position、Rotation 和 Scale。
- 当前追踪的字段保持可见。

### 聚焦与隐藏内容提示

- 全仓库模式展示仓库总览与覆盖进度。聚焦入口、数据或路径时调整画布展示范围，并可调整分析优先级，保留全仓库任务队列。
- 保留独立配色，并通过锁定、高亮和淡化控制阅读重点。
- 隐藏的分支显示数量或明确的展开入口。
- 折叠和筛选后仍能识别当前视图省略了哪些内容。

### 连线规则

- 主体布局尽量从左向右流动。
- 连线自动避让节点和端口文字，并减少交叉。
- 长距离连接可以使用可追踪的跳转标记，支持定位另一端。
- 交叉不表示连接；分叉使用明确的连接点。
- 数据合并通过节点展示具体逻辑。
- 调用、继承和数据传递作为可切换的关系层，明确每类连线的含义。

### 布局稳定与导航

- 展开节点时优先调整附近区域，尽量保持已有节点位置。
- 手动整理的节点位置可以锁定。
- 返回上层或之前的阅读位置时，恢复画布位置、缩放和选中状态。
- 提供小地图、路径导航和阅读书签。
- 阅读视图保存布局、关注范围和选中的数据流。

### 源码依据与分析边界

- 点击连线可查看对应的传参、赋值、读写等代码依据。
- 点击节点可定位源码。
- 虚函数候选目标、动态调用和未解析边界使用明确的文字或状态标记。
- 能区分数据追踪暂时中断与流程确实结束。
- 后续如接入运行记录，应明确区分源码推导的可能路径和某次运行实际观察到的路径。

## 下一轮原型重点

优先验证以下设计在更复杂示例中的阅读体验：

1. 全仓库工作流：覆盖清单、逐模块分析、全局成果汇总、阶段性浏览和断点恢复。
2. 分层展开：原地展开与进入子画布如何衔接，返回时如何恢复上下文。
3. 端口分组：字段较多时如何收拢，并让选中的数据始终可追踪。
4. 稳定布局：展开、收起和切换关系层后，如何保持节点位置与阅读连续性。

当前交互草图使用虚构示例说明画布与数据流交互。实际语言分析能力、框架适配深度和大仓库性能需要通过真实代码样本验证。

## 后续实现方案需要细化的事项

- 全仓库队列、跨模块核对、覆盖统计与恢复点的具体实现。
- 按 Skill 工作规范草案确定图谱的具体格式、校验机制和进度保存方式。
- 画布与 AI 宿主之间如何传递选中节点、问题和分析结果。
- 首版 AI 执行宿主与适配接口的验证，以及与代码编辑器的联动方式。
- 各语言的结构、调用和数据流分析能力如何分阶段落地。
- UE 等复杂项目的构建信息和框架机制如何接入。
- 首个真实仓库及用于检验设计的具体流程。

这些实现事项保留讨论空间；以上已采纳的产品与可读性设计作为后续工作的基线。
