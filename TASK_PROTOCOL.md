# 全仓库任务与批次协议

版本：0.1 · 2026-09-12

本协议已有可运行的参考实现：[core.py](codemap/core.py) 与 [store.py](codemap/store.py)。它提供任务状态转换、事务保存和批次校验，当前 Agent 可通过已安装插件的 MCP 工具或命令入口领取并提交；协议组件本身不执行 AI 推理。

## 1. 全仓库任务的记录

每项任务包含 `id`、`kind`、`scope_ids`、`source_ids`、`depends_on`、`required`、`state`、`attempt`、`lease` 和 `reason`。

| 字段 | 规则 |
| --- | --- |
| `kind` | `inventory`、`analyze`、`link` 或 `update` |
| `scope_ids` | 本项任务要处理的源码实体；不是画布当前可见节点集合 |
| `source_ids` | 必须阅读或核对的源文件 |
| `depends_on` | 已登记任务的依赖，不允许形成环 |
| `required` | 全仓库必需工作为 true；局部聚焦不改变其值 |
| `state` | `queued`、`running`、`blocked` 或 `done` |
| `attempt` | 每次领取任务后递增 |
| `lease` | 当前执行身份与有效期；非执行状态为 null |
| `reason` | 未完成时的接续说明或具体阻碍 |

队列记录所有已发现的必需工作。当前扫描器为每个纳入文件创建阅读任务，并建立依赖全部文件任务的仓库职责和跨文件核对任务。`next` 按构建说明优先及路径顺序选择可领取项；也可明确指定任务。当前会话或画布的本地 Codex 执行器可持续领取和提交；更复杂的优先级调度仍待扩展。

## 2. 领取与执行身份

`claim_task` 或 `GraphStore.claim` 需要任务标识、执行者、当前修订号、当前时间和执行有效期。只有工程为 active、任务为 queued 且依赖全部完成时，才能领取。

领取后分配随机执行标识并保存有效期，任务进入 running。重复领取不会同时获得同一任务；并发存储操作通过事务与修订号检查保证只有一个成功。

这一步只表示协议中已经领取，宿主仍需真正驱动 AI。用户界面的“正在分析”还要结合真实执行连接和活动记录；测试中手动领取任务不能当作 AI 已经运行。

当前提供 `renew` 手动续期命令，不含自动续期和宿主心跳。续期只允许仍有效的执行身份，增加图谱修订号；阅读者应使用返回的当前版本提交。过期身份通过 `recover` 回到队列，不能继续提交旧批次。

## 3. 分批成果

批次具有以下固定字段：

| 字段 | 内容 |
| --- | --- |
| `format_version` | `0.1` |
| `project_id`、`snapshot_id` | 对应仓库工程与源码快照 |
| `base_revision` | 提交所依据的当前工程修订号 |
| `batch_id` | 本次成果的唯一提交标识 |
| `task_id`、`lease_id` | 任务与本次执行身份 |
| `upserts` | 按图谱集合分组的新增或替换记录；每项使用完整记录 |
| `new_tasks` | 本轮发现的必需待办，以 queued、attempt=0 开始 |
| `result` | `partial`、`done` 或 `blocked` |
| `reason` | 接续内容、阻碍或本轮说明 |

批次不能改写任务表中的其他任务，不能修改项目范围，也不能改变当前源码快照。实体与关系使用相同身份可替换记录；新身份新增记录。更新任务可额外提交 `deletes`，按集合列出本任务 `review_ids` 内已核对需要退役的记录；必须说明原因，不能退役文件、目录或仓库清单实体。失去归属、调用上下文或数据产生点的依赖记录一并移除，完整引用校验仍在同一事务内完成。文件移动匹配通过独立的快照迁移完成，普通批次不能修改清单身份。

分析任务只有在声明的实体范围达到目标深度、相关源文件完成阅读与符号登记后，才能提交 done。关系核对任务即使已经交付，未解析关系仍使全仓库报告保留缺口。

## 4. 提交顺序与失败处理

存储组件在一个事务中完成：

1. 读取当前图谱，验证工程与快照。
2. 检查批次是否已经成功提交。
3. 检查当前修订号、执行身份和有效期。
4. 合并实体、端口、关系、依据及新任务。
5. 校验合并后完整图谱的引用和状态。
6. 一起保存有效图谱、任务结果、修订号和批次回执。

任何步骤失败，整个事务回退。用户继续看到上一份有效成果，任务不会因半份结果而计为完成。

准备批次会收集可独立检查的函数必填字段、任务范围状态和当前连接缺少的源码行，返回具体节点、字段、当前值、期望和修复建议。函数详情缺字段与任务范围节点未标为 reviewed/current 是不同问题。反馈最多列出 40 项并报告总数和截断状态；身份或引用无法解析时，后续检查明确保留待检查状态。依赖前置修复的问题可能在下一次准备时才暴露。所有原有图谱、源码、修订和执行身份检查仍保留；没有自动补齐空的语义数组或自动判定任务完成。

相同批次标识和相同内容重试会返回已有状态，不重复累计节点、关系或进度。相同标识对应不同内容时拒绝提交。已成功提交的原批次即使随后执行身份过期，也可以通过回执识别重试。

旧修订号不直接覆盖新图谱。调用方需要读取新版本，检查重叠与依据，再形成新的提交；当前没有自动处理语义冲突的逻辑。单纯刷新修订号只能解决已确认互不冲突的控制变化，不能替代内容核对。

## 5. 暂停与中断

工程设为 paused 后停止领取新任务。已领取任务保留身份；如在有效期内交付，需要对照暂停后更新的工程版本提交。全部活动批次停止后，界面才将“正在暂停”改为“已暂停”。

`recover_expired` 或 `GraphStore.recover` 将过期的 running 任务恢复为 queued，保留已提交成果与接续说明。下一次领取使用新的执行身份，旧执行者的迟到结果会被拒绝。

`partial` 交付会将该任务恢复为 queued，保存说明供下一批继续；`blocked` 保留具体阻碍且仍计入未完成。解除阻碍后可使用 `retry` 重新排队，不能直接把 blocked 改为 done。目录清单缺口通过重新扫描与快照更新处理。

画布请求另存于 `reader_requests`，其状态为 queued/running/paused/completed/failed/needs_attention/cancelled，不与图谱任务状态混用。独立 Codex 阅读进程逐批调用限定 MCP；单例执行身份和心跳控制同一地图的后台进程。每次继续产生新 `run_token`，旧进程不能提交。暂停/取消终止所属进程并释放其任务租约，已提交成果保留。后台失联后显示需要继续，不凭持久队列显示正在执行。问题回答不增加图谱阅读进度；分析请求没有提交且仍有待办时不能标为 completed。

## 6. 阅读布局的保护

阅读视图使用独立存储表，不包含在分析批次中。节点位置、锁定、颜色映射和用户注释不会被批次覆盖。当前提供通用视图对象读写，具体视图格式与同一实体的多个画布实例仍需后续细化。

布局保存不修改分析修订号。快照更新和分析批次都不改写阅读设置。删除节点对应的布局记录继续保留，便于重用同一身份；匹配后的移动保留原来源、文件和符号身份。画布刷新当前图谱后保留仍存在的阅读层、数据聚焦及节点选择，已删除的节点详情会清除。

## 7. 验证范围

测试覆盖重复提交、批次标识重用、旧版本覆盖、失效执行身份、部分结果接续、阻碍项保留、范围缩小拦截、并发领取、失败回退、暂停恢复和布局持久保存。

图谱检查还覆盖端口方向、错接调用上下文、错误调用目标、无效引用、包含与派生环、函数记录深度、源码指纹和证据范围。测试使用人工整理的本地 C++ 示例及临时数据库，未调用真实 AI。

正式 Skill 已连接真实扫描、分段读取与批次命令，插件 MCP 已通过 Codex 实际调用、提交和打开画布的验证。画布本地 Codex 阅读器已实际完成 Python 测试文件提问与分析提交，并接受暂停/继续操作；这不代表复杂多语言仓库已验收。调用工具和续接约定见 [Skill 命令指南](skills/repository-blueprint/references/commands.md)。

## 8. 代码变化后的局部更新

`blueprint_update` 的 `preview` 返回新增、修改、删除、影响范围、完整扫描缺口、`base_revision` 和 `plan_id`。`apply` 使用该修订号与计划标识重新核对当前目录；图谱或源码再次变化时拒绝旧计划，清单有缺口时保留原工程不动。

应用时在一个 SQLite 事务中归档当前图谱并保存新快照。可确定的注释/格式变化保持局部核对；Python 函数体变化以有源码依据的函数为起点沿已记录调用和数据关系传播，接口、未知变化及构建配置保守扩大复核。读取文本或语义证据缺失时不强行收窄。阅读者仍须搜索未登记引用。唯一同内容移动自动关联；明确的一对一 `renames` 可关联有内容修改的移动，保留原身份，路径相关关系仍需复核。

旧快照的活动执行身份失效，受影响文件成为 `update` 任务，原有未完成工作仍然保留。最终仓库核对任务依赖全部文件任务，只要求重读受影响来源并核对相关概况、调用、端口、数据派生及关系。`review_ids` 明确列出应核对的已有记录；done 前记录必须重新有效或明确退役。无文件的完整扫描允许仓库/目录概况使用空清单作为依据，不伪造源文件证据。

当前图谱的旧证据保留原内容哈希并标为过期，不能被新结论当成有效依据。退役记录保存在归档图谱；`source_texts` 按真实内容哈希缓存可读源码文本（沿用 2 MiB 上限），缺失文本不能用新版补造。`history` 列出归档，`compare` 提供记录与源码差异及 `restore_id`；`restore` 要求当前修订号和恢复预览仍一致。恢复在事务中归档当前图谱、建立新修订、保留阅读设置，并针对今天的源码重新排队核对，不修改仓库文件。活动阅读者会阻止恢复。快照更新只登记新版本与待办，执行由当前 Agent 或显式画布请求接续。
