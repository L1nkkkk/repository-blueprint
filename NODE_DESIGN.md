# 节点的种类与视觉区分

版本：0.1 · 2026-09-12

节点种类、任务归属、分析状态和数据流身份分别表达。画布根据种类选择标题、图标和结构；连线颜色表示任务，具体数据使用端口名称、版本和稳定流标识区分。

## 节点种类

| 种类 | 外观与默认内容 | 展开后的内容 |
| --- | --- | --- |
| 仓库、目录、模块 | 容器样式，顶部小页签；仓库、目录与模块各有图标和文字标签 | 成员分区及跨边界连接；模块可关联多个目录 |
| 文件 | 文档样式、文件图标、语言与扩展名 | 文件中的声明、定义及关联类型 |
| 命名空间、包 | 括号或包图标，标题标明作用域 | 所属类型和可直接归属的函数 |
| 类、结构体、接口、枚举 | 类型图标、明确种类标签，成员区分栏 | 字段、方法和类型关系 |
| 函数、方法 | 函数图标、输入输出端口卡片 | 参数、返回值、调用、条件与内部数据变化 |
| 字段、参数、变量、值版本 | 紧凑圆角样式，名称、类型和值版本；读写端口分开 | 来源、读取、写入及必要的对象上下文 |
| 运算 | 运算图标、表达式或一句话变换说明 | 每个输入与派生结果的关系 |
| 外部接口 | 虚线边框、外部接口图标和边界说明 | 已知契约、传入传出内容与可解析范围 |
| 待识别内容 | 问号图标、原始名称与待核对原因 | 识别后保留身份并更新种类 |

种类目录保存在 [node_kinds.json](codemap/node_kinds.json)，由 `kind` 映射到外观家族。家族用于减少视觉样式数量，具体种类仍以文字区分，例如类和接口属于同一外观家族。

## 附加角色与状态

`entry`、`event`、`test`、`tool`、`generated`、`vendor` 和 `public_api` 是用途或来源标签。例如：

- 程序入口：函数节点 + “入口”标签。
- 测试用例：函数节点 + “测试”标签。
- 生成的头文件：文件节点 + “生成”标签。
- 实现某个接口的类：类节点 + 实现关系；保持类的外观。

阅读与分析进度用独立状态行表示；推断和候选用依据标签表示；旧源码结果用“需要更新”标签表示。暂停分析、完成分析或源码过期都不改变节点种类。

外部接口的虚线边框表示范围边界，不表示它必然未知。仓库内部的动态调用不确定时，原函数仍是函数，通过关系的候选或未解析标签表达不确定性。

## 颜色与端口

标题使用低饱和的种类色、小图标和标签；节点主体维持统一背景。任务颜色用于端口、实际连线、公共入口与任务图例，同一任务的所有输入、派生结果和子调用保持同色。图例以色点和单行任务名紧凑排列，入口职责放在悬停提示和点击后的详情中；点击可多选并高亮任务线路。悬停连线显示所属任务、用途和传递的数据名称。Dxx 只保留在数据详情或端口提示中。复用节点显示多个任务色标；没有明确任务归属的关系保持灰色。

实际关系使用连续曲线与实心箭头，箭头在输入端口外侧独立绘制，常用缩放下保持固定屏幕尺寸；缩至全景时适度减小，以免相邻端口的箭头重叠。源码变化、候选关系与走线拥挤不再让实际关系变为虚线：待核对的关系数在图例旁显示，每条线的提示和详情保留具体状态，拥挤提示继续单独显示。只有组织输入的灰色流程归属线使用无箭头虚线。

数据流另有沿实际曲线移动并随切线转向的箭头。聚焦时只播放选中范围；灰色流程归属线没有流动箭头。用户可以暂停或开启动效，偏好随工程保存；减少动态效果的系统偏好影响首次默认值，页面不可见时暂停动画。它只说明传递方向，不表示执行状态或耗时，源码核对状态仍独立保留。

数据端口为圆形，关系或调用端口使用不同形状及文字。函数调用顺序与数据依赖分别保存、分别显示，不因端口位置相邻而建立关系。

折叠容器的端口需要映射到内部端口；端口聚合保留成员，展开可定位。类默认显示分组成员，仅在流程视图需要时显示其汇总输入输出。

可选简洁模式保留图标、种类标签、端口与角色标签，减少标题色。节点缩远后仍能根据轮廓和图标识别大类。

## 浅色与深色主题

提供“跟随系统”“浅色”“深色”三种选择，默认跟随系统。主题入口放在工程栏中，适用于画布、导航、节点、详情、源码区域、小地图和表单控件。

- 浅色使用较浅的画布背景与清晰的文字、边界；深色使用深灰背景和适当提亮的文字与连线。
- 节点种类的轮廓、图标和角色标签保持一致。标题种类色和任务颜色分别按主题调整明度，保持同一任务对应的色系。
- 切换主题不改变节点位置、缩放、选择、数据聚焦和分析进度，不重新安排布局。
- 未解析关系、过期依据和分析状态仍有文字或形状提示，避免仅凭颜色识别。
- 正式阅读器需要保存主题偏好，并在“跟随系统”模式下响应系统变化；主题偏好不写入源码事实或分析版本。

独立交互示意提供页面内的三种主题切换。现有本地画布阅读器已将主题、节点位置、锁定和各视图的缩放保存到地图工程，可在重新打开时恢复。

## 与源码的对应

文件归属、代码语义归属和架构分组分别记录。例如 C++ 声明与定义可能对应多个文件，同一个类使用一个实体身份关联它们。

运算节点必须来自实际表达式或有明确依据的解释；不能为了排版补出不存在的处理步骤。一个函数内部的表达式节点不额外算作一个已分析函数。

图谱中的实体身份与画布实例分开：同一函数可以同时出现在两个阅读视图，也可以在不同调用上下文中出现。两个位置不应自动变成两份独立源码实体。

## 已实现的调用分层与布线

数据流默认按调用逐层展开。在调用方画布中，一个调用卡片只展示与当前层相连的输入、返回和写回端口；通过“展开调用内部”进入该次调用，面包屑与返回按钮恢复原层的布局和缩放。两次调用分别保存上下文，即使调用同一个函数也不会混在一起。

画布增加“本层数据”“输入边界”“结果边界”和“函数体”等展示角色。这些是原实体与端口的阅读视图，不会新增源码实体、虚构处理步骤或增加分析进度。圆角数据供给卡、虚线输入/结果边界和普通调用卡相互区分；源码种类和分析状态继续独立显示。

同一上下文内，只有一个明确来源、身份保持不变且不是本层新产生的值，才允许把接收与继续传递合并为一条可视连线。有多个来源、派生新值或会隐藏反馈关系时保留边界。合并线保存全部原关系及证据；点击连线可查看中转、写回、值版本和派生来源，并打开对应源码。通过展开各层或切换“展开全部上下文”，可以核对未压缩的关系。

节点默认按数据依赖从左向右排布，环状依赖单独处理。互不连通的流程分区排列，同层节点依据两侧邻居调整顺序，减少可避免的交叉，并为曲线绕行留出间距。“一键整理”重排当前画布并清除当前连线的手动整理点，保留锁定节点，再适配选中任务或整个画布；“撤销整理”恢复之前的位置、整理点和视角。每层布局与一次整理前的备份保存在阅读设置中，其他层的布局保留。

前向连接与需要绕行的连接都使用平滑曲线，检查节点障碍，并对重叠线段增加绕行代价。返回与写回由端口标签和连线详情表达，几何上向左不等于代码循环。点击线后可添加一个整理点，拖动调整，双击或按 Delete 移除；无法通过的重叠节点或整理点会显示提示，不会丢弃关系。锁定节点仍可能阻挡走线，需解锁或移动后再整理。算法有搜索上限，尚未完成大型仓库的密集布线与性能验证。

上述展示和坐标策略属于画布实现。repository-blueprint Skill 负责提供有依据的语义关系与数据身份，不规定节点坐标或连线形状。

## 已实现的流程入口与多输入

“流程阅读”是从处理场景出发的入口。一条流程可以包含多份输入和多条数据流。当前使用已记录的根分析上下文确定流程范围，具体调用作为子流程；文件、架构分组以及不同请求或事件不会仅因复用同一函数而合并成一个流程。

本层增加一个圆形流程入口，灰色无箭头虚线连接各输入。入口和虚线是阅读视图中的组织元素，不是新函数、数据生产者或控制关系，不影响源码事实、图谱版本和完成进度。输入来自已记录的调用边界、本层提供的值或明确的外部数据源；不能把流程内部计算出的结果重新算成独立输入。

每份输入以紧凑卡片显示名称、原始端口和值身份，沿用任务颜色并保留各自源码依据。同一端口的同一份数据被多个节点使用时复用输入卡片；名称相同的不同数据保持独立。灰线表示归属，彩色箭头表示实际传递，共用任务颜色不会让几份输入共用一个数据身份。

点击入口清除数据聚焦，显示本层全部输入和处理；点击输入沿既有 `derived_from` 关系追踪后续派生结果，其他独立输入淡显。追踪状态显示数据名称，点“恢复任务全部线路”返回任务聚焦。派生结果继续使用任务颜色。展开子流程后继续追踪，面包屑返回上层，顶部“查看整条流程”返回根流程。每个层级独立保存位置、锁定、缩放和整理点；根任务颜色在层级切换、重开与增量更新后保持稳定。

没有记录数据的调用仍作为本层成员保留，不补造参数或调用先后顺序。尚未整理流程的工程显示等待整理，完整文件清单与独立模块仍可从仓库结构浏览。流程归属目前依赖阅读者正确划分分析上下文；尚未提供任意业务场景的自动发现或跨根上下文合并界面。

## 已实现的全仓库拓扑

“拓扑总览”与仓库层级、单次调用展开分别提供阅读入口。可按文件夹、文件、类型与作用域、架构模块或源码实体归组；物理位置、语义归属和架构归属按各自轴查询。文件夹展开到文件，文件及其他组展开到成员实体。独立工具、未连通模块和未读实体保留在相应组中，不只显示入口可达范围。

组卡片展示成员数与当前关系层中的内部关系数。组内关系折叠保留，可在详情中逐条核对源码；跨组关系按方向、类型、数据身份和调用上下文聚合，并生成可追溯到内部端口的显示端口。多重归属无法得到唯一代表时保留原实体，不任取一个模块。每条原关系只归入一条汇总边或一个组内集合；切换归组不会改写图谱。

调用、数据、控制流与引用依赖分层显示。当前视图统计分流、汇入和未连通节点，同一邻居之间多条线不重复算分流。调用环按源码实体识别，提示“可能递归”；控制环按分析上下文分别识别，仅使用当前且有源码依据的关系。返回和状态写回有独立标签，不能从几何回线推导代码循环。没有控制关系时显示尚未记录控制流。

拓扑各归组、关系层和展开组合独立保存布局。不同调用位置之间的平行调用线错开连接位置，并在连线详情显示调用位置。当前实现仍加载完整图谱并限制单视图最多 150 个节点；强连通分量算法的长链测试不代表大图界面的性能验收。

## 早期交互示意

全仓库工作流中增加“节点样式”阅读页，并将模块、函数和字段样式应用到已有流程。种类对照页展示独立样本，无源码归属或调用含义；工程栏可切换主题，全仓库任务和原有数据流交互保留。

文件栏与详情栏可独立拖动调宽或收起，双击分隔条恢复默认宽度。宽度、收起状态保存在工程的阅读设置中，不改写节点坐标和源码事实；缩小窗口时给画布预留空间，窄屏使用可关闭的浮层。重新选择节点或连线会打开详情。

源码依据使用随明暗主题切换的语法配色，保留行号、原始文本和依据行背景。配色库随工具本地打包，在独立 Worker 中处理；读取片段时同一份已校验源码的前文用于保持跨行字符串、注释的颜色。文件版本不一致时仍拒绝作为当前依据展示，未支持的语言或配色超时退回纯文本，不影响画布操作。
