# 代码蓝图：完整使用流程

版本：0.1 · 2026-09-12

状态：完整目标流程规格。真实仓库扫描、任务保存、源码阅读、MCP 调用及本地画布已可用；画布可直接发起本机 Codex 分析，独立保存请求与回答。已提供分类覆盖、增量影响、文件移动关联和历史比较/恢复；复杂多语言及大仓库能力仍待验证。当前可执行用法见 [安装与调用指南](INSTALL.md) 与 [工作台指南](WORKBENCH.md)，以下未覆盖的目标仍需逐项验收。

图谱和任务存储已有参考实现，范围与限制见 [图谱格式](GRAPH_FORMAT.md)、[任务协议](TASK_PROTOCOL.md)；节点的视觉区分见 [节点规范](NODE_DESIGN.md)。

本文把 [整体设计](DESIGN.md) 与 [Skill 工作规范](SKILL_SPEC.md) 转成用户可以实际操作的流程。工程交付、依赖与验收要求见 [首版交付规格](V1_SCOPE.md)。

## 1. 首版采用的使用方式

首版建议由现有 AI 会话承接仓库梳理，使用配套的本地画布阅读器查看成果。AI 按 Skill 阅读代码，任务管理组件保存队列和中间成果，画布读取同一份仓库地图工程。

这是为了让首版具体可实现而采用的建议；独立桌面外壳、编辑器插件和具体 AI 宿主尚未定案。图谱与阅读视图独立保存，后续更换外壳可以复用。

用户最少只需提供可访问的仓库位置，并表达“梳理整个仓库”。工具自动发现语言、模块与多个入口，建立全仓库任务。用户已经明确全仓库范围后，普通模块切换和批次继续无需逐次确认。

画布直接发送任务的功能依赖实际接入的 AI 执行器。首版完整交付必须验证至少一种接入；没有连接时，仍可浏览已保存的成果，并明确提供“复制分析请求”，由用户带回可访问该仓库的 AI 会话。

## 2. 主流程

| 阶段 | 用户操作 | 工具与 AI 的工作 | 用户看到的结果 |
| --- | --- | --- | --- |
| 建立工程 | 提供仓库，选择“全仓库梳理” | 检查访问、识别已有工程、记录源码版本与范围 | 工程名称、仓库位置、分析范围、执行连接状态 |
| 启动分析 | 在 AI 会话发出要求，或在已连接的画布点击“开始梳理” | 登记任务，建立完整清单和模块队列 | 已接收或等待连接，随后进入实际分析状态 |
| 形成全景 | 可以先浏览，不必等待最终完成 | 整理文件与符号骨架、模块职责和候选入口，持续逐模块深入 | 全仓库结构、模块状态、当前工作与剩余范围 |
| 深入阅读 | 展开模块、切换组织方式、查看入口流程 | 加载已保存的细节；全仓库分析继续 | 文件、类型、函数、输入输出与相关源码 |
| 追踪数据 | 选择端口、字段或连线 | 沿已知传递和派生关系形成阅读视图 | 数据来源、变化、分支、读写、消费位置与未解析边界 |
| 继续提问 | 对选中内容输入问题 | 查询已有证据，必要时提高相关分析任务的优先级 | 有依据的解释，以及本次新增或修正的图谱内容 |
| 暂停与续接 | 点击暂停，之后继续 | 保存已提交成果和恢复点，恢复时核对源码与队列 | 已保存的进度、剩余工作和可恢复的阅读位置 |
| 查看交付 | 打开成果与覆盖报告 | 核对范围、分析深度和跨模块关系 | 全景、层级图、模块说明、入口流程、数据关系、覆盖与缺口 |
| 更新代码分析 | 源码变化后点击“更新分析” | 标记受影响内容，重新阅读并核对关联结论 | 更新范围、失效或修正的关系，原有阅读设置继续保留 |

## 3. 建立工程与启动

### 默认设置

| 项目 | 首版默认行为 |
| --- | --- |
| 分析模式 | 全仓库梳理；局部追踪作为辅助入口 |
| 分析深度 | 源码到函数级职责、输入输出、直接调用、成员读写与数据传递、派生关系 |
| 必填内容 | 可访问的仓库位置；来自当前会话的明确仓库可以直接沿用 |
| 可选内容 | 用户优先关注的模块、问题与已知构建配置；优先关注不缩小全仓库范围 |
| 保存位置 | 工具工作目录中的独立地图工程；提供“打开保存位置”和迁移入口 |
| 原始代码 | 阅读并记录依据；分析结果和用户注释写入地图工程 |
| 已有成果 | 仓库身份和版本可核对时复用，并列出需要更新的部分 |

普通文本源码、测试、脚本、构建配置、生成源码与随仓库提供的第三方源码均登记处理方式。入口无法到达的代码仍在队列中。文档和资源按其内容整理说明或依赖关系；二进制、缺失依赖及不可访问内容保留边界。

版本管理历史、工具自身输出等非项目源码不作为函数分析对象。实际的排除项及原因在范围清单中可见，不能计入已读源码。忽略规则中的文件仍须检查是否属于应纳入的项目源码。

首次启动不强制用户回答“从哪个入口看”。构建配置无法确定时记录条件和候选分支，继续能够推进的工作；只有缺失信息确实阻碍后续分析时才提出具体问题。

### 启动状态

点击操作后先显示“请求已发送”或“等待执行连接”。执行器确认接收且开始工作后才显示“正在分析”。同一操作重试时复用请求标识，避免建立重复工程或重复全仓库任务。

执行器不可用时，操作区显示原因与可行入口，例如“复制全仓库梳理请求”。复制请求会带上工程位置和用户意图，不能显示为已启动。

## 4. 全仓库分析过程

任务按以下工作顺序推进，允许相互穿插核对：

1. 建立完整范围清单和当前源码记录。
2. 识别模块、类型、函数、多个入口与公开接口，形成可导航的骨架。
3. 逐模块阅读函数实现，整理职责、输入输出、分支、副作用与数据变化。
4. 回查跨模块调用、对象状态、事件和消息等连接。
5. 汇总各类阅读视图，核对遗漏、错误连接和过期依据。
6. 对照覆盖清单形成最终报告，保留未达到要求的分析项。

模块排队顺序可以受用户关注点影响。独立模块、测试和工具必须获得实际处理机会；建立清单后长期不再推进的范围仍属于未完成工作。

每批提交一份完整、可校验的增量。画布继续显示上一份有效图谱，待新批次提交后更新；失败或中断的半份输出不会混进当前成果。提交后自动进入下一批，直到完成、用户暂停或实际执行条件中断。

### 进度展示

进度面板同时提供以下信息：

| 信息 | 显示方式 |
| --- | --- |
| 当前任务 | 正在整理的模块，以及“读取实现”“核对连接”等工作内容 |
| 范围登记 | 清单是否已枚举完成；未完成时显示“正在发现范围” |
| 源码阅读 | 已读源码文件数及已知总数，可展开到具体文件 |
| 分析深度 | 达到目标深度的函数数、部分分析数、尚未分析数；不支持的内容单列 |
| 关系核对 | 已核对、候选、未解析、待重新核对的关系数；注明“当前已发现关系” |
| 队列与恢复 | 待处理模块、跨模块待核对项、最近保存时间和执行状态 |

没有完整分母时不显示全仓库百分比。随着阅读发现新的符号或关系，分母可以增长，界面说明“本轮新发现内容”。用户可以从每个数字进入相应清单。

不会用“已经读完全部文件”替代“所有分析已经完成”。仍可继续处理的必需任务存在时，整体保持未完成。

## 5. 画布的主要区域

| 区域 | 放置内容 |
| --- | --- |
| 顶部工程栏 | 工程与源码版本、全仓库模式、任务状态、开始或暂停或继续、更新分析 |
| 左侧导航 | 全景、文件结构、代码结构、入口流程、数据追踪；全局搜索与书签 |
| 中央画布 | 当前层级的节点、端口、连线、路径导航、缩放与小地图 |
| 选中详情 | 职责、输入输出、源码、关系依据，以及围绕该内容继续提问 |
| 可展开进度区 | 当前批次、覆盖范围、剩余队列、未解决项和近期更新 |

默认让画布占据主要空间。详情随选择出现，进度保持一行摘要，用户需要时再展开。窄窗口下导航和详情改为可切换面板，保留当前阅读位置。

### 全景与层级

初始全景覆盖仓库所有已登记分区，包括未分析模块。节点明确区分“仅已定位”“部分分析”“已核对”“需要更新”。模块名称已出现，不代表其内部关系已分析。

按文件位置、代码语义和架构职责提供关联视图。用户可以从文件找到其中的类型与函数，也可以从类型跳到声明和实现文件；不强迫所有语言经过同一条层级链。

缩放调节当前层级的信息密度。小范围使用“展开内部”在原位查看，大范围使用“进入模块”打开子画布；选择哪种操作由用户明确触发。返回时恢复位置、缩放、选择和聚焦的数据流。

画布只加载当前可见层级及相关数据，未显示内容保留数量和入口。大模块的折叠不会从全仓库分析范围中删除其成员。

### 节点、连线与源码

节点默认显示名称、种类、一句话职责和分组端口。调用、数据、继承等关系分层展示；关系层的选择会显示在画布上，避免混淆含义。

点击节点查看解释和源码位置；点击连线查看对应的传参、返回值、赋值或读写依据。详情提供本地源码片段；有已配置的编辑器联动时可打开对应文件位置，否则提供路径和行号。

用户可以拖动并锁定布局、添加显示别名和注释。AI 更新不覆盖这些阅读设置。手动备注有独立作者和来源标记，不能自动变为源码事实。

## 6. 追踪一条数据

以“追踪这个字段的去向”为例：

1. 用户选择输入端口、参数、字段或已有连线，选择“向下追踪”；也可选择“追溯来源”。
2. 画布显示当前数据名称、所属对象或调用上下文及固定标识。
3. 已确认的传递、分支与派生结果沿用所属任务的颜色。计算产生的新数据使用独立名称和版本，并能沿派生关系继续追踪。
4. 经过折叠模块时，通过边界端口进入其内部位置；返回上层继续保留所追踪的数据。
5. 到达写入、消费、返回或明确边界时标出作用；未解析的调用显示“去向尚未确认”。
6. 用户可锁定多条数据对照，也可对边界发起进一步分析。

同名参数、不同对象和不同调用位置按实际上下文区分。共享状态的写前值和写后值分别呈现。循环和递归使用回边或可展开的重复结构，并保留路径条件，不无限展开节点。

颜色表示任务归属，端口名称、数据版本与方向说明实际传递。任务图例紧凑显示色点和名称，悬停或点击在详情中查看入口职责，点击图例或彩线高亮任务的相关线路；多个任务可以同时选中。实际关系用连续曲线和实心箭头显示，待核对状态在图例计数、连线提示及详情中查看。输入或端口支持单份数据追踪，“恢复任务全部线路”清除数据聚焦。更新分析不会为已有任务重新随机分配颜色。

流动箭头沿数据方向移动，跟随当前聚焦范围，可在画布右上角暂停或开启。它是方向示意，不代表运行状态或真实耗时。拖动后布线混乱时，点击“一键整理”重排当前节点并重新布线；锁定节点保留原位。“撤销整理”可以恢复整理前的节点、走线点和视角。

## 7. 在节点上继续提问

用户可以直接提出“这里为什么改写位置”“这个值最终传到哪里”“继续分析这个模块”。系统记录工程、图谱版本、选中实体或数据、调用上下文、用户问题和优先级。

| 内容状态 | 用户操作后的行为 |
| --- | --- |
| 已有有效成果 | 直接展开或引用已有源码依据回答；无需为查看而重新分析 |
| 部分分析或尚未分析 | 将必要工作加入或提升到现有队列，并显示等待或执行状态 |
| 依据已过期 | 先核对受影响源码，再给出新结论；原结论仍以过期状态可查看 |
| 无执行连接 | 生成可复制的上下文请求，保留待处理状态 |

局部问题复用全仓库图谱。任务管理会核对重复范围，避免把同一函数加入多个互不关联的待办。回答后回到剩余全仓库工作；局部问题的完成状态和全仓库的完成状态分别显示。

新增内容通过更新摘要进入画布。用户当前视口、锁定位置和已有颜色保持稳定，新增节点优先安排在相关位置。

## 8. 暂停、中断和恢复

| 情况 | 状态与恢复行为 |
| --- | --- |
| 用户暂停 | 显示“正在保存”，停止领取下一批；当前可提交批次完成保存后显示“已暂停” |
| 强制关闭或执行失败 | 保留最近有效批次，未提交批次恢复为待处理；重新连接时核对执行状态 |
| AI 连接不可用或执行会话停止 | 显示“等待恢复”及原因，不继续显示正在分析 |
| 缺失依赖或读取失败 | 记录具体范围与原因，先继续能够独立处理的模块；需外部信息的任务保留 |
| 恢复分析 | 读取工程、队列和恢复点，核对源码变化，再接续剩余任务 |
| 关闭画布 | 图谱和阅读设置先保存；任务是否继续由实际执行器状态决定 |

保存恢复点与持续运行是两种能力。Skill 本身规定流程，持续领取任务需要实际运行的执行器；宿主、连接或执行资源不可用时只能保留进度并等待恢复。首版界面必须如实反映这一点。

在已连接状态下，用户只需发起一次全仓库任务，不必在每批之间点击继续。显式暂停、实际中断或必要条件缺失时才进入恢复流程。

## 9. 源码变化后的更新

打开工程、恢复任务、开始新批次以及用户请求更新时，核对相关文件与分析配置。尚未核对的版本显示“待重新核对”。

变化会影响当前文件的实体、直接相关关系及依赖这些结论的上层汇总。沿已记录的依赖追踪受影响范围；范围无法确定时扩大核对，并明确仍待确认的部分。

新增文件进入覆盖清单和全仓库队列；删除文件的实体保留历史关联，当前图谱标记移除。重命名和移动优先匹配已有身份；无法可靠匹配时保留待确认项，不能仅靠名称强行连接。

更新提交前核对依据版本。阅读中变化的文件和结论标为待重新核对，不把不同源码状态拼成“全部有效”的图谱。

用户看到的是“哪些地方变了、哪些需要更新、这次更新了什么”。地图工程保留布局、书签、颜色和注释；源实体消失后，关联的阅读内容进入待处理清单。

## 10. 全仓库交付与结束状态

| 状态 | 显示与判断 |
| --- | --- |
| 部分成果 | 范围尚未登记完整，或仍有未读源码、未达到深度的实体、可执行的必需任务 |
| 等待恢复或信息 | 必需任务仍在，但当前执行条件不可用；列出具体原因和剩余范围 |
| 阅读已覆盖，仍有未解析项 | 全部范围内可读源码已有阅读记录，但部分关系无法确认；不能显示“全部梳理完成” |
| 全仓库梳理完成 | 约定范围达到目标深度，必需核对项已处理，成果一致且依据有效；明确列出边界与排除项 |

成果页提供仓库全景、完整层级、模块说明、入口流程图集、数据关系图集及覆盖报告。每项都能进入相应图谱或清单。静态分析结论限定在记录的源码与配置下，不表示观察到了每一种实际运行路径。

完成报告必须可追溯到具体源码与任务。把失败项改名为“未知”、隐藏未读代码或仅生成符号索引，均不能达到全仓库完成标准。

## 11. 一次完整体验的验收场景

以一个包含程序入口、核心逻辑、输出模块、未从入口调用的独立模块、测试和构建脚本的小型完整仓库为首个样本：

1. 用户只指定仓库并要求全仓库梳理，所有分区自动进入清单和队列。
2. 第一批成果可浏览，用户无需逐模块安排后续任务。
3. 从总览进入文件和函数，选中数据能查看其来源、转换、写入与消费依据。
4. 在分析过程中提出局部问题，问题得到处理后剩余全仓库任务继续。
5. 暂停并恢复，已完成结果不重复生成，未完成批次能够重做，颜色与阅读位置保持。
6. 修改一个样本函数，再更新分析；相关关系被核对，旧依据不会继续显示有效。
7. 未读模块和未解析关系可从报告定位；只有达到目标深度的内容才计入完成。

交互演示中的仓库、源码与进度均为虚构数据，仅用于讨论操作和状态。真实分析、持续执行和大仓库性能必须通过以上场景及后续语言、规模样本验证。
