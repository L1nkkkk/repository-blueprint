# 代码蓝图：首版交付规格

版本：0.1 · 2026-09-12

状态：实现范围与验收草案。当前已有可安装的 Codex 插件、正式 Skill、12 个 MCP 工具、任务保存及真实图谱画布，完成 Codex 工具连接与读写验证；固定小型 C++ 样本已核对。全仓库拓扑、流程阅读及原工程局部更新已加入。画布已支持本机 Codex 请求、独立后台执行、覆盖分类、文件移动关联及历史比较/恢复。五种语言完整样本与大仓库能力仍待验证。

关联文件：[整体设计](DESIGN.md)、[Skill 工作规范](SKILL_SPEC.md)、[完整使用流程](USER_FLOW.md)。

## 1. 首版必须跑通的体验

用户指定一个完整仓库，AI 按统一规范自动逐模块阅读，保存带源码依据的结构化图谱。用户可以边分析边浏览全景，深入节点、追踪数据、提出局部问题；任务能够暂停续接，并在代码变化后更新受影响结论。

全仓库是首版的默认范围。首个验证仓库可以小，但须覆盖其完整范围，包括主入口之外的代码、测试与工具。UE 规模的性能与复杂关系作为后续扩大验证的要求保留，不以单条流程演示代替全仓库能力。

## 2. 首版工程组织建议

首版采用“Skill + 本地辅助工具 + 地图工程 + 本地画布阅读器 + 一个已验证的 AI 执行连接”。以下是职责边界，不预设具体界面框架、数据库或 AI 平台。

| 组件 | 输入 | 输出或职责 | 首版交付物 |
| --- | --- | --- | --- |
| 仓库梳理 Skill | 用户意图、仓库、工程记录、待处理任务 | 阅读、解释、证据核对与分批整理规则 | 正式 Skill 及按需读取的语言参考 |
| 阅读辅助工具 | 范围和具体文件、符号 | 完整清单、文件内容、版本记录、符号与引用查询 | 可重复调用的本地工具及失败说明 |
| 任务与进度管理 | 全仓库范围、目标深度、优先关注点 | 模块队列、跨模块队列、任务状态、恢复点 | 可持久保存并恢复的任务组件 |
| AI 执行连接 | 待处理任务与必要上下文 | 接收确认、执行状态、批次成果或错误 | 至少一个在目标宿主中实际验证的适配器 |
| 图谱存储与校验 | 批次实体、端口、关系、数据流、证据 | 有版本的有效图谱及变更说明 | 数据格式、读写与校验工具、增量提交机制 |
| 画布阅读器 | 图谱、阅读视图、进度 | 分层浏览、数据追踪、源码详情和分析请求 | 可加载真实成果的本地阅读器 |
| 验证样本与报告 | 人工可核对的完整样本仓库 | 漏项、错误连接、恢复与更新结果 | 验收场景、检查记录及能力说明 |

分析内容、用户阅读设置和执行状态分别保存。每份成果绑定仓库身份、源码依据版本和图谱版本。图谱按模块或实体集合分片，允许只加载当前阅读范围，避免把整份大型仓库及所有源码一次交给 AI 或画布。

## 3. 地图工程需要保存什么

初版字段与序列化格式已记录在 [图谱格式](GRAPH_FORMAT.md)，任务规则见 [任务协议](TASK_PROTOCOL.md)。以下内容属于首版必需的逻辑交付，当前参考实现尚未覆盖全部生产能力。

| 内容 | 必需信息 |
| --- | --- |
| 工程说明 | 仓库身份与位置、分析范围和模式、源码记录、配置、成果版本 |
| 覆盖清单 | 文件分类、语言、源码指纹、目标深度、阅读状态、明确的排除或失败原因 |
| 结构与解释 | 文件和符号、归组、职责、输入输出、分支和副作用摘要 |
| 关系与数据 | 端口、关系种类、数据身份、派生来源、调用上下文、对象和值的必要区分 |
| 源码依据 | 支持具体结论的位置、版本、必要片段和关联依据 |
| 任务记录 | 模块阅读与跨模块核对队列、执行尝试、当前状态、优先级、恢复点 |
| 增量历史 | 批次标识、依据版本、新增或修正内容、校验结果、提交状态 |
| 阅读设置 | 节点位置和锁定、颜色、展开状态、选择、书签、别名和注释 |
| 成果报告 | 全局说明、入口与数据视图索引、覆盖结果、未解析项和更新说明 |

稳定实体标识要能关联声明和实现，记录移动与重命名的匹配结果。源码位置或名称发生变化不能直接导致所有节点换身份；匹配不确定时保留可核对的状态。

颜色绑定根上下文所表示的任务，子流程继承任务颜色；数据身份与版本独立保留。位置绑定阅读视图中的实体身份。关系事实与视图中折叠出来的汇总连线分开记录，汇总线可以展开到其实际成员。

## 4. 执行与接续约定

Skill 负责工作方法，任务组件负责持久记录，实际执行器负责领取任务并推进 AI 阅读。是否能够持续运行，由宿主和适配器的实际能力决定。

### 最小任务循环

1. 核对仓库与成果版本，领取一个有边界的模块阅读或关系核对任务。
2. 加载相关成果、源码和待核对问题，执行阅读与推理。
3. 提交带任务标识、依据版本与源码证据的批次成果。
4. 校验引用和版本，提交有效增量，同时更新对应任务与覆盖记录。
5. 保存恢复点，按全仓库队列继续领取任务。

长函数或大模块可以拆批，但原任务保持剩余内容记录。局部提问可以提高优先级，全局队列保持可见并继续获得处理机会。

### 中断与一致性

- 已提交批次通过批次标识去重，重复发送不会生成重复实体或重复进度。
- 增量与对应进度应一起提交，不能出现“图谱尚未保存，任务已经完成”。
- 未提交批次在中断后回到可恢复状态，重做时检查已有成果。
- 执行状态有最近活动记录。失联后重新确认执行状态，显示连接中断或等待恢复，避免永久显示正在分析。
- 保存的恢复信息包括剩余范围、关联成果和需要核对的源码；不能只保存一句自然语言总结。
- 多个请求基于旧图谱同时返回时，先检查版本和重叠范围，再合并或重新核对。

## 5. 画布与 AI 的最小交互契约

以下为行为契约，接口命名与传输形式待实现验证。

| 请求 | 必要上下文 | 结果 |
| --- | --- | --- |
| 开始全仓库梳理 | 仓库与工程、分析范围、目标深度、可选优先事项 | 唯一任务、接收状态、范围清单与后续进度 |
| 查看或展开 | 工程、图谱版本、实体与阅读层级 | 已保存的视图数据；无须触发 AI |
| 追踪数据或提问 | 工程与版本、实体或数据、调用上下文、方向或问题 | 现有结果或必要任务，以及有依据的回答与增量 |
| 暂停或恢复 | 工程、全仓库任务、操作标识 | 保存状态、恢复位置、剩余队列 |
| 更新分析 | 工程、源版本与配置变化 | 受影响范围、新任务、增量与更新说明 |

所有分析请求区分“已发送”“已接收”“执行中”和最终结果。画布动作不以本地计时器假装执行成功。

无连接状态提供可复制的请求与上下文，继续允许离线阅读。这个回退方式必须在界面中明确；首版完整交付不能仅靠复制文本通过“画布直接继续分析”的验收。

## 6. 首版画布范围

### 必须可用

- 浅色、深色和跟随系统主题，覆盖全部阅读区域，保存偏好并保持阅读状态。
- 全仓库总览与范围清单，独立模块、测试、工具及未分析内容有入口。
- 无限平移与缩放、节点拖动、明确的展开和进入子画布、返回恢复上下文。
- 文件位置、代码语义和架构分组三种关联导航，搜索实体与入口。
- 固定节点格式、分组输入输出端口、按类型切换关系层。
- 稳定的数据流颜色、单条或多条聚焦、派生关系和明确的未解析边界。
- 源码片段和依据位置，编辑器联动不可用时提供可定位的信息。
- 稳定布局、锁定、小地图、书签及阅读设置保存。
- 真实任务进度、阶段成果、暂停续接与节点提问入口。
- 更新说明、过期依据状态和用户注释保护。

### 后续可扩展

- 更多 AI 宿主与编辑器的直接接入。
- 桌面应用外壳、团队共享与协作批注。
- 运行记录导入，用于展示具体执行中观察到的路径。
- 更多导出格式、复杂布局偏好和跨仓库统一视图。

以上扩展不替代首版的全仓库范围、数据关系和证据要求。

## 7. 语言与规模的验证顺序

产品目标包括 C++、Python、C#、TypeScript 和 JavaScript，支持情况按实际能力分别记录。

| 验证阶段 | 样本与重点 | 可以形成的结论 |
| --- | --- | --- |
| A：完整闭环 | 小型多模块 C++ 仓库，包含入口之外的代码、测试、工具与构建文件 | 全仓库队列、读取整理、图谱浏览、恢复与更新能否连通 |
| B：语言覆盖 | 为其余四种语言分别使用可人工核对的完整样本 | 各语言的结构、调用、数据、边界处理是否通过验证 |
| C：复杂关系 | 扩大 C++ 和各语言样本，覆盖间接调用、共享状态、回调与项目特有组织方式 | 识别哪些关系可核对，哪些需要辅助信息或保留候选 |
| D：UE 与大型仓库 | 真实 UE 仓库及大型样本，登记构建目标与可访问内容 | 范围覆盖、复杂框架适配、批次成本、恢复能力和画布性能的实际结果 |

阶段 A 的闭环原型和完成语言覆盖后的首版分别报告。未验证的语言仍是目标能力，不能标为“全面支持”。UE 全仓库任务中，优先验证某个模块只是阅读顺序调整，其他模块仍保留在队列和完成要求中。

对不支持的语法或无法定位的函数，文件仍留在清单中并显示限制，不计为已达到函数级分析深度。源码关系仍需要实际阅读与证据核对，辅助工具的索引输出不能直接等同于正确数据流。

## 8. 验收条件

| 编号 | 场景 | 通过条件 |
| --- | --- | --- |
| A1 | 仅给仓库并要求全仓库梳理 | 无需指定入口，所有分区进入清单；主入口之外的代码实际被处理 |
| A2 | 分批推进 | 一次启动后自动续批，成果持续更新，未处理范围不会消失 |
| A3 | 函数级梳理 | 达到深度的函数具有职责、输入输出、调用、成员读写与依据；不适用项有说明 |
| A4 | 跨模块数据 | 传参、返回、派生与写入可从图中回到源码，不混接同一函数不同调用的输入输出 |
| A5 | 不确定关系 | 已支持、候选、未解析及过期依据可区分，推断不会显示为运行事实 |
| A6 | 画布阅读 | 从全景进入函数再返回，视口和已锁定布局保持；同一任务及其子流程保持颜色 |
| A7 | 节点提问 | 请求到达真实执行器并有反馈；局部工作结束后全仓库剩余工作继续 |
| A8 | 中断恢复 | 在批次提交前后分别中断，不丢已提交成果，不重复累计进度，未提交工作可恢复 |
| A9 | 源码变化 | 修改、增加、删除或移动样本文件后，受影响关系被标记并核对，旧证据不会继续显示有效 |
| A10 | 完成判断 | 隐藏一个未读模块或保留必需待办时不能显示完成；报告能定位缺口 |
| A11 | 无连接阅读 | 可浏览保存的图谱；任务按钮准确显示连接状态，不模拟真实执行 |
| A12 | 语言与性能 | 能力声明对应已通过的样本；记录首次可浏览耗时、批次耗时、内存和交互延迟及测试环境 |
| A13 | 主题切换 | 浅色与深色下节点、文字、连线和状态可辨认；切换保留布局、缩放、数据聚焦与任务状态，重新打开保留主题偏好 |

语义验收使用人工核对的预期调用与数据关系，并回读源码。结构校验检查标识、引用、端口、证据位置和版本。两类检查分别记录；界面演示不能替代真实分析与执行验证。

大型性能指标先在明确设备和样本上测量，再制定发布门槛。发布时说明已测试的仓库规模和配置，不用未经测量的节点数或时长承诺替代证据。

## 9. 实现顺序与当前状态

| 顺序 | 工作 | 当前状态 | 依赖 |
| --- | --- | --- | --- |
| 1 | 产品需求、全仓库规范、完整使用流程与首版范围 | 已有规格草案；交互示意使用虚构内容 | 后续通过样本修正规格 |
| 2 | 图谱具体格式、稳定标识、任务状态与增量协议 | v0.1 格式、校验与提交参考实现已通过基础测试；已支持快照迁移、文件移动身份关联、旧图谱归档与恢复，分片待实现 | 当前规格 |
| 3 | 正式 Skill、阅读工具、任务保存与校验 | 15 个 MCP 工具提供扫描、本地结构索引、读取、搜索、领取/续期/恢复/提交和局部更新，命令入口保留；结构字段可通过 symbol_id 复用 | 数据与任务协议 |
| 4 | 真实成果驱动的画布与一个 AI 执行适配器 | Agent → MCP → 图谱 → 画布已验证；共同入口与多输入的流程阅读、子流程、数据追踪、拓扑归组、主题与布局可用；画布 → 本机 Codex 请求、真实进度、暂停与继续已接通 | 图谱读写和任务接口 |
| 5 | 小型完整仓库闭环与恢复、更新验证 | 五文件 C++ 样本、MCP 单文件读写与局部更新闭环通过；修改实现后的输入派生、断言差异及布局保留已验证；本机 Codex 已实际完成独立 Python 小样本的提问与分析提交，复杂项目自主分析仍需扩大验收 | 前述组件可用 |
| 6 | 各目标语言覆盖、复杂关系和 UE 规模验证 | Python / C++ / C# / TS / JS 常用结构解析已验证；复杂语义和 UE 全仓库规模待验证 | 闭环通过并逐项补足能力 |

当前已接通画布真实 Agent 请求、独立后台分批执行、文件/场景/未确认关系分类覆盖，以及更细的更新影响、文件移动关联和历史比较/恢复。下一步按语言扩大完整项目验收，再实现大仓库按需加载并测量性能。Python 函数体影响收窄不等于五种语言语义解析已齐备；UE 能力须通过专门样本确认。当前验证及限制见 [验证记录](VALIDATION.md)。

## 10. 后续实施计划

下面保留总体实施顺序。阶段 1–4 的基础链路已实现并在小样本验收，复杂项目准确性、多语言能力及大型仓库性能仍需扩大验证。完成与否依据实际运行，不以规格或演示代替。

| 顺序 | 下一阶段 | 具体工作 | 阶段完成后用户能做什么 |
| --- | --- | --- | --- |
| 1 | 接入真实仓库 | 实现完整目录清单、文件分类、内容指纹、读取与搜索、范围缺口记录和工程初始化 | 指定仓库位置，看到真实全仓库范围及待分析内容 |
| 2 | 正式 Skill 与实际执行 | 打包阅读与输出规范；接入一个实际 AI 宿主；补齐自动领任务、分批接续、执行有效期维护、阻碍解除和跨模块核对 | 发起一次全仓库梳理，AI 自动逐模块推进，进度与成果可保存和恢复 |
| 3 | 真实图谱画布 | 读取已保存成果，接入增量；实现层级归组和端口聚合、搜索、源码联动、稳定布局、阅读视图与主题偏好保存 | 浏览自己的代码，追踪数据，在节点上提问并收到真实结果 |
| 4 | 代码更新与首次全仓库验收 | 实现新源码快照、影响范围、实体身份匹配、过期依据和局部重读；用完整样本验证主入口外代码、调用上下文、恢复和更新 | 代码修改后更新地图，并从覆盖报告核对整个仓库是否达到分析要求 |
| 5 | 多语言验证 | 分别完成 C++、Python、C#、TS 和 JS 的真实完整样本，补齐语言参考与辅助分析能力 | 使用有明确验证范围的多语言梳理能力 |
| 6 | UE 与大型仓库 | 适配项目组织与构建信息，处理复杂关系；落地分片存储、按需加载和大图布局，测量性能及分析成本 | 对 UE 等大型仓库持续推进全仓库梳理，按层级流畅浏览并明确看到分析边界 |

第一个样本使用小型、多模块 C++ 项目，并包含独立代码、测试和工具。样本规模用于控制首次验证工作量，全仓库范围与函数级分析要求保留。阶段 1–3 形成首次真实使用链路，阶段 4 核对恢复、更新和完整交付；五种语言和 UE 的能力在各自样本通过后再标记可用。

进一步的扩展包括独立桌面外壳、更多编辑器与 AI 宿主接入、导出与共享、跨仓库关联以及运行记录对照。这些扩展排在真实全仓库链路与可靠性验证之后。
