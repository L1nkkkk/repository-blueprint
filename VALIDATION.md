# 当前验证记录

## 2026-09-14 · 准备批次的具体诊断与修复反馈

领取任务附带 `preparation_contract`，包括函数六项详情的未填写模板和任务完成条件。准备失败仍是 MCP `isError=true`；返回明确的问题代码、类别、节点、字段、当前值、期望和修复动作。函数缺字段与任务范围节点未标为 reviewed/current 分开检查；在引用能解析时，与未读源码范围一起收集。独立问题最多展示 40 项，保留完整计数、截断标志及尚待执行的依赖检查。缺行反馈保留实际区间，不把字段错误当成重读要求。没有自动补齐语义数组或修改完成状态。

Python 全套 150 项：149 项通过，1 项因 Windows 符号链接权限跳过；JavaScript 48 项通过，工作台语法检查通过。新增检查覆盖多函数缺字段与任务/来源状态共同报错、真实缺行与字段问题共同报错、未知引用和重复身份、错误报告截断、修正字段后复用已读材料、原始批次与图谱校验的相同约束、版本和租约拦截、旧版 MCP 文本客户端以及实际独立进程的结构化错误与修复提交。补充最终反馈措辞及原始提交位置后，10 项诊断测试另行通过。

独立通用包与 ZIP 的 60 项载荷文件通过哈希校验，实际入口提供 17 个工具。在三行 Python 样例上，第一次 prepare 同时返回五项问题：函数缺条件数组、输出数组未填写、文件节点审阅状态未完成，以及文件阅读/声明登记状态未完成。修正这些字段后第二次 prepare 成功，再 commit 成功；失败后的新增源码读取次数为零。第一次失败前后图谱相同，也没有保存准备草稿。结果与实际错误 JSON 保存在开发机 work/prepare-diagnostics-validation，未调用模型。

本次为通用 MCP 实现与独立样例验收，没有重跑 WB/OpenGL 审阅，不能据此承诺具体提速比例。新包独立放置，未改写 WB 正在使用的安装目录或重启其连接。不同来源/版本/执行身份错误仍需按真实情况修复，依赖前置修复的错误可能在后续准备时才暴露；既有统计不会重新分类历史错误。

## 2026-09-14 · 通用审阅耗时与进度统计

新增第 17 个 MCP 工具 `blueprint_metrics`，与画布“Agent → 审阅耗时”和 CLI 共用报告。采集位于通用 MCP 层，独立保存每次连接的本地工具耗时、调用间隔、完整源码行重叠、相同参数失败重试和成功批次回执。当前领取范围与文件阅读进度仍来自图谱。不会把调用间隔宣称为模型推理耗时，也不从扫描、工具调用或提交次数推算审阅完成。

Python 最终全套 139 项：138 项通过，1 项因 Windows 符号链接权限跳过；JavaScript 48 项通过，更新后的工作台脚本语法检查通过。新增检查包括单调时钟、阅读包与单独读取的重叠、来源指纹变化、同连接跨地图隔离、重连后的统计选择、准备批次归属与重复回执、未结束记录、统计写入失败时实际提交成功、报告分页、参数与源码正文不入统计库，以及真实发行包的 stdio、画布 HTTP 与 CLI 报告兼容。

独立浏览器样例验证深浅主题、窄屏布局、两次连接切换、任务/调用明细展开、复制报告及没有历史记录的旧地图提示，浏览器未报告错误或警告。页面导出按钮已触发；当前内嵌浏览器的下载文件落盘未确认，复制 JSON 与 CLI 报告可用。样例只有本地工具操作，没有模型审阅；未改动 WB 的连接或 OpenGL 地图。

本机 Windows / Python 3.12.14 对四份源码副本做热身后交替测量，每轮读取 20 次、各 5 轮：有统计的中位耗时 235.25 ms，无统计为 48.75 ms，差值折合约 9.33 ms/次调用。图谱前后相同。此结果仅覆盖本地源码读取，不含 MCP 传输、模型、网络或真实 WB 审阅，不能据此给出总体提速比例。复测入口为 `python scripts/benchmark_metrics.py --output 新目录`；本次原始结果位于开发机的 work/metrics-validation/benchmark/benchmark.json。

独立通用发行包及 ZIP 的 59 项载荷文件全部通过收据哈希校验，实际启动后提供 17 个工具，MCP、HTTP 和 CLI 返回同一连接统计；原安装副本 62 项文件仍与原收据一致。未覆盖正在使用的安装副本。WB 在当前批次成功提交后切换新版 MCP，才能从下一次调用开始采集；过去的三小时和旧进程不能补记。真实 WB / OpenGL 全流程的耗时瓶颈尚待这份新统计提供证据。

## 2026-09-13 · 模块阅读包

`blueprint_next` 默认附带第一页阅读包；新增 `blueprint_pack` 可按任务、文件或节点读取、分页续接，MCP 共 16 个工具。画布阅读者、外部 Agent 接续说明与 Skill 已接入相同流程。包内包含实际编号源码、缓存的声明结构、关联文件候选、完整保存记录和待办；只有实际返回的完整源码行进入当前连接的证据读取记录。阅读包不调用模型、不领取额外任务，也不推进审阅状态。

Python 全套回归 124 项：123 项通过，1 项因 Windows 符号链接权限跳过。随后补充领取与组包之间的版本变化保护，模块阅读包 16 项及实际 stdio 集成 11 项全部通过。新增检查覆盖字符预算、中文长文件连续分页、超长行缺口、容器范围、跨文件线索、源码/版本变化、连接间读取隔离、已有证据的双端来源核对、完整记录与超大记录检索提示、无解析器文件、限定阅读者以及组包失败后保留原任务租约。本次没有修改画布资源。

从本项目原样复制 project.py、core.py、repository.py、store.py，在独立地图上测量：主文件加三份关联文件片段共 436 行，分 3 页，第一页组装 9.58 ms，全部组装 21.33 ms；各页紧凑 JSON 分别为 25,534、27,214、18,688 字符。图谱和完成状态保持不变，没有调用模型。结果见 work/pack-validation/benchmark.json，样例页见 work/pack-validation/reading-pack-example.json。这只测量本地材料组装；尚未测量使用阅读包后完整 AI 审阅的耗时，不能据此承诺把三小时缩短到某个时长。

默认最多三份关联文件，每份先提供 80 行；主文件读取至末尾，缓存声明与语法点先展示各 20 项并给出继续查询位置。关联发现只使用已有关系/调用上下文和可定位的本地导入路径，导入扫描至多八个主文件；未记录的调用方、类型解析、构建别名、宏及动态调用仍需核对。超长行、超大元数据、索引缺失与范围截断均明确报告；游标耗尽不表示语义审阅完成。通用包和安装副本的最终哈希及 stdio 验收记录保存在 work/pack-validation/release-checks.json。

## 2026-09-13 · 本地结构解析

Python 全套 109 项：108 项通过，1 项因 Windows 符号链接权限跳过；JavaScript 39 项通过。新增验证覆盖五种目标语言的声明、参数、嵌套、重载与源码范围，语法错误、缺失依赖、版本缓存失效、原生解析失败和超时隔离，已有实体身份复用、索引节点的 Agent 接续及真实读取后的简明提交。移动后的发行包通过真实 stdio 调用结构索引与 structure 指南。Python 使用 AST，原生语法包的固定版本见 requirements-parsers.txt。

在更新源码前的本项目清单上实测：75 个清单文件中有 48 个代码文件，46 个受支持，2 个 PowerShell 文件明确报告暂不支持。首次结构索引 2,856.97 ms，未改动文件的重复检查与缓存复用 97.33 ms；生成 1,459 个声明与 8,254 个未解析语法调用/导入点。图谱摘要前后相同，没有改变任何阅读进度。详细数据保存在 work/parser-validation/benchmark.json。该测量包含源文件哈希核对与原生解析进程开销，不是 AI 全仓库审阅的速度测试，也不是 UE 规模性能保证。

浏览器实测一键解析、缓存复用、文件内函数展开、参数展示和高亮源码跳转。revision 383 的已审阅图谱副本经结构投影后，458 个已存实体复用身份；原图谱与覆盖状态不变。主工程 revision 384 的已完成任务和当时的 75 个源文件另行备份到 work/parser-validation/before-integration，供本次源码更新后核对。

Tree-sitter 0.26.0 在 Windows 解析真实 app.js 时发生原生访问异常；固定为通过回归的 0.25.2，且原生解析在独立进程执行，单文件 15 秒超时，失败不会结束 MCP 服务。索引按约 8 秒分页预算让出控制，保留后续游标。初次初始化预解析至多 100 个文件；大仓库通过画布、CLI 或 MCP 游标继续全部范围。参数是语法声明元数据，不自动变成输入端口；宏、类型、反射及运行时数据流仍需语义核对。

## 2026-09-13 · 审阅传输、成果复用与批次准备

Python 全套 96 项：95 项通过，1 项因 Windows 符号链接权限跳过。新增检查覆盖简明返回的计数和截断、完整模式兼容、分段读取缺口、函数信息缺失、引用解析、草稿不可变、断线后提交、重复提交、版本/源码/租约拒绝、有效结论的磁盘核对，以及画布阅读者的任务归属。实际 stdio 进程完成准备、断开、另一连接检查并提交同一准备批次；不用重复传输批次正文。上下文和端口复用的已有证据也纳入提交时的磁盘核对。

在主地图 revision 227 的临时 SQLite 备份上，对同一结果用相同 JSON UTF-8 序列化比较：状态从 76,965 字节降至 2,518 字节（减少 96.73%）；领取 codemap/repository.py 从 74,596 字节降至 9,838 字节（减少 86.81%）。不含 JSON-RPC 外框及全仓库快照扫描，未测量真实模型耗时。测量脚本与结果在 work/efficiency-validation；原图谱 SHA-256 保持 45e026c09f4ebfb89e2e0f882b4d1028e60fdb232f3b7f9ea92a7eeba5816f37，25 项完成、45 项待办不变。

MCP 现在提供 14 个工具。简明模式不隐藏未完成数量，完整记录仍可按 ID 查询；prepare 的读行记录仅覆盖当前连接，不能凭搜索、旧快照或缺失片段创建新证据。prepare 不推进进度，commit 仍走原协议。查询复用只核对已记录结论的来源，不证明未知依赖或新的调用场景。全仓库并行调度、持久模型会话及 UE 规模性能尚未实现或验证。

已安装 0.5.0+codex.20260913053542；官方插件和 Skill 校验通过。通用副本 47 项文件、插件副本和安装缓存各 51 项文件的收据哈希一致，三份入口实际启动后均提供 14 个工具及 preparation 指南。新包 work/repository-blueprint-efficient.zip 含 48 项、215,326 字节，全部哈希通过。8767 画布服务已重新加载，图谱 API 和连接 API 正常；源码改动提示保留，未改写原分析快照或启动已取消的阅读请求。

## 2026-09-13 · 通用 MCP 接入

Python 全套 84 项：83 项通过，1 项因 Windows 符号链接权限跳过；JavaScript 39 项通过。通用发行包不含 Codex 插件清单或安装器。两份宿主配置可直接启动独立 stdio 客户端，完成读取、校验提交及下一客户端复用证据并领取剩余任务；包移动后重新生成的连接路径正确。规范可通过 workflow 工具指南读取。

画布验证外部 Agent 默认入口、Claude Code/Gemini CLI 配置切换、全仓库接续说明和复制成功。生成说明不创建后台请求、不更改图谱。修复源码变化时根场景引用的证据未传播过期状态而导致画布无法加载的问题；使用真实自审工程确认可加载，保存图谱哈希保持不变。

当前是标准 MCP 与本地浏览器验收，未在真实 Claude Code/Gemini CLI 模型会话中运行阅读。外部会话的聊天和执行控制归宿主管理。全仓库执行性能优化仍未完成。

## 2026-09-13 · v0.5.0 工作台

Python 全套 81 项中 80 项通过、1 项因 Windows 符号链接权限跳过；JavaScript 39 项通过。新增请求与恢复 API、运行身份隔离、暂停续接、变更分类、文件移动匹配和历史恢复检查通过。

本机 Codex 实际完成独立 Python 仓库的源码提问、节点分析提交和后台分批接续；在浏览器暂停、刷新、继续后完成两项剩余任务。历史恢复通过页面操作并检查了源码、原身份及布局保留。文件已读、场景已整理、函数未纳入场景和未确认关系分开显示。详细范围及原始记录保存在开发机的 work/feature-review/ACCEPTANCE.md，不随公开仓库发布。

插件 0.5.0 已安装，官方插件验证、安装收据与源码内容校验、已安装入口的 12 个 MCP 工具及历史 compare 实际调用通过。真实模型测试不构成复杂多语言或 UE 规模验收。

## 2026-09-12 · 以下为此前各阶段记录

日期：2026-09-12。环境：Windows，Python 3.12，标准库运行实现。本记录针对本地开发版本，不是多语言或 UE 规模的发布认证。

## 自动测试

运行 `python -m unittest discover -s tests -v`：共 67 项，66 项通过，1 项跳过。跳过项是真实符号链接创建测试，当前 Windows 进程没有创建符号链接的权限；没有将其计为通过。

测试覆盖完整范围登记、五种目标语言的文件分类、独立代码/测试/工具/第三方与 Git 忽略文件、显式排除、目录访问失败、扫描过程中目录变化、文件哈希失败、超大与非文本源码的阻碍状态、UTF-8/UTF-16 行号读取、空文件依据、搜索截断与源码变化、重复初始化保护、任务领取与续期、暂停/恢复/阻碍重试、证据失败时回滚、重复提交、调用上下文隔离及布局保存。

本地服务测试核对真实图谱和源码响应、主题保存不改变分析版本、路径越界请求拒绝、外部来源请求拒绝、缺少写入标识的修改拒绝，以及源码变化后的过期显示。

画布脚本通过语法检查。正式 Skill 通过 skill-creator 的格式校验；该校验不证明任意 AI 宿主的行为正确。

运行 `node --test tests/canvas.test.cjs tests/topology.test.cjs tests/workflow.test.cjs`：26 项通过。覆盖分层接口端口、两次调用的数据隔离、各层展开后覆盖全部 17 条原始数据关系、合并线的证据/过期状态、歧义和新值不误合并、叶函数不虚构输入输出关系、含环的依赖布局与锁定位置、障碍绕行、返回通道、自连接、窄通道、手动整理点及避免折返重叠。

流程新增 8 项检查：一个入口组织五份独立输入；本层产生的新位置不误记为入口输入；子流程的三份传入数据与原始依据保留；同值分流复用卡片、同名异值保持分开；复用相同函数的两个场景隔离；输入追踪派生结果而不吞并其他输入；明确的外部来源纳入流程、结果消费者保留；布局锁定、筛选和无数据调用不虚构关系。源图在投影前后保持不变。后端 52 项重新运行，51 项通过、1 项跳过。

拓扑检查覆盖五种归组及展开前后关系、数据身份、证据和调用上下文的完整保留，独立模块不丢失，多重架构归属不强行匹配，平行边不重复统计分流，静态调用环与控制环区分。候选与过期关系不用于确认环，不同上下文的反向控制边不拼成循环。迭代遍历通过 12,000 节点长链与大环测试；这只是算法栈安全检查，不构成 UE 规模界面性能验证。

新增 MCP 测试使用重定位到含中文临时路径的实际发行内容，验证 stdio 初始化、工具列表、错误后连接继续可用、源码阅读、失败批次不推进、有效提交、重复提交、分页版本守卫、任务控制、画布服务关闭重开与布局保留。`control` 关系通过实体端点与源码证据校验，端口端点被拒绝。

## 插件安装与 Codex 连接

使用官方 Plugin Creator 辅助脚本注册个人市场，安装副本位于用户目录的 `plugins/repository-blueprint`；Plugin Creator 与 Skill Creator 格式校验通过。MCP 入口使用本机 Python 和该副本，不依赖原开发项目目录。

流程阅读版本 0.3.0 已重新打包并安装，状态为启用。安装副本与发行目录的 29 个文件均通过收据哈希校验，ZIP 的 30 个条目与收据一致且无运行数据库或字节码缓存。更新后通过 Codex 再次连接全部 11 个工具，重新打开已完成样本，确认指南、状态及画布中的新流程资源均可读取；此次复验复用已有成果，没有重复声明一次新的自主分析。

通过本机 Codex app-server 的临时会话实际连接插件：状态为 `connected`，11 个工具可调用；Skill 列表中 `repository-blueprint:repository-blueprint` 已启用。经 Codex 调用指南、初始化、领取、阅读、提交、状态与画布工具，逐行核对单文件 Python 样本 `DEFAULT_SPEED = 2` 并提交证据；文件任务和仓库核对任务完成，画布 HTTP 返回已保存成果。

该验收使用真实 Codex MCP 传输和人工可核对的小样本，没有发起额外模型轮次，因此证明宿主可调用、保存与展示，不代表通用模型已自主梳理未知仓库。MCP 连接结束后其画布服务停止；重新调用启动新地址，工程和布局仍然保存。

## 固定 C++ 样本

真实枚举并逐行核对 `examples/sample_repo` 的全部 5 个文件：运动实现、独立诊断实现、测试入口、工具代码和 CMake 构建说明。4 个源码文件共包含 5 个显式函数/方法，均整理了职责、输入输出、调用、读写和条件。声明与语义层级、构建文件也已覆盖。

源代码核对结果通过实际领取、批次提交和 SQLite 保存流程进入图谱，最终 6 个任务完成，报告为 `complete`。`examples/review_sample.py` 保留这些已核对结果的重放逻辑，用固定哈希拒绝不匹配的源码；它不是通用自动分析器。

数据场景核对第一次 Move 与第二次 Move 的不同调用上下文：第一次写回的新位置成为第二次的旧位置，两个速度参数不混接，各自嵌套的 IntegratePosition 调用也分开记录。图谱包含 7 个数据身份、17 条数据/读写关系、4 条普通调用关系和 1 条宏引用。`assert` 保留为标准库宏边界，未伪造为普通函数调用。

结果位于 `examples/reviewed-map-v1.json`；早期 `reviewed-map.json` 保留首次阅读成果，`sample-map.json` 则是有意保留待办的协议样例。后续使用与演示默认读取最新样本重放生成的工程。

这些结论来自源码阅读与推导。当前环境未配置 C++ 编译器，没有声称编译或运行过 C++ 样本。

## 浏览器验证

已通过可用的浏览器控制入口打开真实本地服务，并检查：

- 5 个实际文件及测试、工具标签，完成状态与任务数量。
- 文件 → 命名空间 → 结构体/类/函数的展开和面包屑。
- 两次调用分别显示；全展开数据层为 6 个显示实例、17 条关系，调用层为 5 个实例、4 条关系。新增默认分层数据视图：main 层为 5 个显示节点、7 条可视连线，对应 9 条原始关系；每个 Move 内部为 3 个节点、4 条可视连线，对应 8 条原始关系，跨层边界可在两层各显示一次。
- 节点职责、输入输出与源码依据，点击 Move 的依据能看到匹配版本的第 10–13 行。
- 浅色、深色、跟随系统主题；重新加载后保留所选主题及已保存的缩放。
- 适应画布、小地图及连线路由；修复摘要文字溢出与相邻数据身份颜色过近。
- 390 像素窄窗口下可打开文件面板，进入独立诊断模块，再显示详情面板；验证后恢复默认窗口尺寸。

本次布线改动还在浏览器中检查了 Move 的调用内部、合并线的返回与写回依据、对应第 10–13 行源码、整理点的添加/拖动/双击删除、节点移动后重新布线，以及锁定节点在自动整理后位置不变。重新加载后保留当前调用层、节点位置与整理点；测试整理点已移除，移动的样例节点已恢复自动布局。全展开的 17 条数据关系可见，当前样例没有布线失败提示。

拓扑总览已在真实页面检查：`src` 文件夹展开到文件，`motion.cpp` 再展开到实体；组内调用可打开对应第 10–13 行证据。文件夹数据层为 9 条跨组关系与 8 条组内关系，保留全部 17 条原始数据关系；写回详情保留具体调用位置。浅色与深色下数据颜色、聚焦和跨组端口可见，刷新保留拓扑选择；未记录控制流时显示明确提示。两次调用线在同一对组节点之间分别显示，详情标明第 7 行和第 8 行的调用位置。

尚未建立完整自动化浏览器回归套件。此轮几何与视图测试不构成任意仓库、任意密集布局或 UE 规模的验证。

流程阅读的浏览器检查：根流程显示一个圆形入口、五份输入、两次 Move 子流程和后续断言边界；五条灰色归属线与七条彩色数据连线分别表达组织和传递。点击第一次速度输入时高亮该输入与两次派生位置，其他输入淡显；源码依据显示第 7 行的实参。展开第一次 Move 后显示三份调用方输入，保持追踪与调用位置，点击子流程入口恢复本层全部显示。明暗主题下均已检查；刷新保留子流程、主题与缩放，“查看整条流程”返回根流程并显示全部输入。

## 工具自身扫描

工具自身也执行了全目录扫描，显式排除 `work` 运行成果目录和 `**/__pycache__` 字节码缓存；每个排除边界保存在清单中。源码、文档、Skill、界面资源和样例成果均进入清单。扫描后已读数仍为 0，不能因建立清单而显示“代码已经分析”。

## 尚未通过的完整产品验收

没有验证画布主动唤起 AI、未知仓库的模型自主分析验收、会话结束后的持续执行、移动/重命名身份匹配、全部五种语言的语义分析或 UE 规模。当前交付是“可安装插件 + Skill + MCP 读取、提交与局部更新 + 真实成果画布、拓扑和流程阅读”。下一步补画布主动提问连接，再推进新仓库自主阅读、多语言样本与大仓库能力。

## 原工程内的局部更新（0.4.0）

0.4.0 已安装并启用；安装副本与发行目录的 30 个文件、ZIP 的 31 个条目均通过收据哈希校验，插件与 Skill 格式校验通过。通过 Codex 再次连接 12 个 MCP 工具，实际调用新增更新预览，重新打开已完成样本并读取画布资源。此次安装复验未发起额外模型轮次。

新增 13 项增量检查，覆盖修改后的依赖影响、无关模块保留、旧图谱事务归档、布局与颜色保护、计划过期与源码再次修改、幂等重试、清单缺口阻止迁移、新增和删除、旧执行身份拒绝、未核对记录阻止完成、限定范围退役与依赖清理、构建配置影响扩大、无文件仓库核对，以及共享摘要依据过期但不扩展无关文件。另新增本地 HTTP 更新入口和重定位发行包的 MCP 更新调用检查。

实际样本位于 `work/incremental-demo`：复制已核对的五文件 C++ 仓库，仅将 `Move` 的写回改为 `next + 1.0f`。界面预览识别一个修改文件，影响范围为运动实现和测试两份文件；通过真实页面应用更新后，已读源码从 4/4 变为 2/4，新增两项文件重读与一项关系核对，独立诊断和工具保留已读成果。

`examples/review_update.py` 以精确内容指纹限定这份新样本，并通过真实领取、源码读取和提交重放已核对的更新结论。只读两份受影响文件；分别保存积分返回值与加 1 后的写回值，原 D4/D7 数据颜色身份保留，新增 D8/D9 积分结果。按源码推导位置依次为 1、3、6，原断言仍要求 4，结果摘要明确记录不一致。所有任务最终完成；这不是 C++ 运行结果，也不是未知仓库的模型自主分析验收。

浏览器验证覆盖深色变化面板、实际更新按钮、浅色子流程、过期提示清除和新函数体节点显示。自动接收新成果后保留 `Move :7`、速度输入聚焦、锁定的 `IntegratePosition` 和 70% 缩放；依据面板显示新版本第 12 行 `state.position = next + 1.0f;`。刷新后保留主题、子流程与数据聚焦，再检查显示版本一致且禁止重复创建更新任务。

影响计算按已记录关系在文件粒度保守传播，未记录依赖仍由阅读者搜索补齐；构建配置变化会扩大重读范围。移动暂按删除和新增处理。旧图谱保存在数据库中，未保存历史源码字节，也未提供交互式还原入口。

## 2026-09-14 持久骨架层验收

M1–M4 的实现、边界与实测数据见 [SKELETON_ACCEPTANCE.md](SKELETON_ACCEPTANCE.md)。最终 Python 167 个用例（166 通过、1 个现有 Windows 符号链接权限用例跳过），前端 48 个用例全通过。11,340 行真实项目中，首次构建 2.37 秒，重复同步零重解析，五类查询 P95 均低于 20 毫秒；百万行规模尚未实测。


## 2026-09-14 规模路径阶段 A

A1–A4 按顺序完成，每阶段完整回归后分步提交。最终 Python 175 个用例（174 通过、1 个现有 Windows 符号链接权限用例跳过），JavaScript 48 个全通过。包括 FTS 定向更新、12 轮随机边重算差分、精简格式磁盘校验、写锁/版本竞争、同步分阶段回滚、物化入度与目录索引、小型并发一致性。详见 [阶段记录](SCALE_PROGRESS.md) 与其中的原始输出和时间戳。独立 mypy 十万行基准尚未执行，体积/性能门禁尚未判定。
