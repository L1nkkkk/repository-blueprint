# 分析工作台

打开地图后，顶部增加“Agent”“覆盖”“历史”。节点、颜色、布线和侧栏设置继续保存在原工程中。

左侧文件清单：单击定位并高亮画布中的对应文件、函数或折叠模块；双击或在详情里点“展开文件内部”进入文件。选择画布节点时，左侧同步标记其来源文件。

## 发起与继续分析

“Agent”现在提供两种执行方式：默认“外部 MCP Agent”，以及可选的“本机 Codex 后台”。

两条入口均使用简明 MCP 返回、已有成果核对和准备批次提交。更新工具后，外部 Agent 重新连接 MCP，本机后台在下一次执行时使用新流程。可以要求“按 preparation 指南继续已有工程，先复用有效结论，再补读并提交剩余任务”。既有地图无需重建；源文件发生变化时仍须先登记更新。每批限于当前任务，缺少的跨文件工作保留具体待办；模块并行调度仍未实现。

### 外部 MCP Agent

1. 选择 Claude Code、Gemini CLI 或其他 MCP Agent，展开“首次使用：查看 MCP 接入配置”。复制配置并把该服务合并到对应宿主设置；具体位置见 [安装指南](INSTALL.md)。
2. 选择节点后提问或点“继续拆解”，生成带节点和来源范围的接续说明；“生成全仓库接续说明”带上当前地图和整个剩余队列的要求。
3. 复制说明，发给已经连接工具的 Agent。Agent 可用 `blueprint_guide(topic="workflow")` 读取共同规范，再查询、领取、读取和提交。无需在每个宿主另装一份 Skill。
4. Agent 提交后，当前画布显示同一份图谱。换 Agent 时继续提供原地图绝对路径，已提交成果和布局继续使用；未提交的推理过程不在图谱里。

生成说明只准备上下文，不会创建后台请求或启动模型。回答及停止操作由外部宿主管理；画布中的“本机 Codex 请求记录”只记录本机 Codex 后台任务。连接配置和绝对路径仅适用于运行仓库的这台机器，复制到另一台机器后需重新生成。

### 本机 Codex 后台

1. 选择节点，在详情中输入问题，点“让 Agent 回答”。回答基于实际读取的源码，注明文件与行号；只提问不会改变阅读完成状态。
2. 点“继续拆解”，把该节点及相关文件交给阅读器，结果须经过图谱校验后保存。
3. 顶部“Agent → 继续全仓库梳理”接续工程的全部剩余任务。阅读器每批领取、阅读并提交，再处理下一批；没有新提交且仍有任务时停在“等待继续”，不会显示完成。
4. 面板显示真实执行记录与回答。“暂停”结束当前阅读进程，保留已提交成果；“继续”领取新的执行身份；“取消请求”结束该请求，已提交成果仍在地图里。

请求和结果持久保存在地图数据库。后台进程独立于聊天与画布服务；关闭它们后已启动的分析可以继续，重开地图可查看结果。电脑关机、进程失联或登录失败会中止实际执行，之后需继续请求。它不是开机服务，也没有定时启动功能。全仓库队列若被外部工具设为暂停，须先恢复工程队列；请求暂停与工程暂停是两个层次。

需要本机已登录的 Codex，使用账号额度与模型设置。找不到程序时显示连接说明并保留请求。详见 [安装指南](INSTALL.md)。当前适配器只允许读取当前地图对应仓库，并通过限定的 MCP 校验写入图谱；不执行仓库程序、不改源代码。

## 审阅耗时与基线

在“Agent → 审阅耗时”选择一次已记录的 MCP 连接。新版工具从首次访问地图时自动采集，适用于 WorkBuddy、Claude Code、Gemini CLI 等外部 MCP Agent 和本机后台阅读器。页面每 5 秒刷新，可导出当前连接的 JSON 报告；`blueprint_metrics(project=地图目录)` 和 `python scripts/run.py metrics 地图目录` 读取相同报告。查询统计本身不增加调用计数。

| 指标 | 定义 |
| --- | --- |
| 工具实际耗时 | 单调时钟测量的本地工具执行时间，按建立清单、结构解析、领取/组包、读取/搜索、查询、准备、提交等分类；不含统计写入和网络传输 |
| 调用间隔 | 同一连接两次已记录调用之间的间隔；可能包括模型处理、网络、人工停顿、其他地图或其他工作，不能当成纯推理耗时 |
| 重复返回源码行 | 同一连接中相同来源 ID、相同内容指纹的完整行重叠，包含阅读包与单独 read 的重叠；重新核对可能合理 |
| 失败重试 | 同一连接再次发送完全相同参数、且上次该请求失败的次数；修改参数后的重试不归入这个数字 |
| 已观测成功批次 | 本连接成功返回的不同批次回执数，重复提交同一批次只算一次；不表示完成文件数 |
| 参数/返回字节 | UTF-8 JSON 大小，不含 MCP 协议外壳；不是 Token 消耗，错误响应正文不计入返回字节 |

当前领取范围和阅读完成数来自图谱，统计本身不推进它们。未收到结束记录、长时间无新调用，只表示观测缺口，不能据此判定宿主仍在执行或已经卡死。宿主名称来自客户端初始化声明。每次重连是独立统计连接；本机后台多批可能对应多个连接，当前不自动把不同连接合并为一次全仓库运行。

统计保存在地图目录的 `review-metrics.sqlite`，与图谱数据库、布局、版本和租约分开。仅保存工具、时间、计数、来源/任务标识和错误分类；不保存源码、提示词、参数正文或响应正文。统计写入失败时仍返回实际工具结果，并在进程诊断输出说明记录失败；报告会保留可见缺口。

建立基线时，固定同一源码清单、分析范围、起始成果、模型和推理设置，并记录冷/热缓存条件。报告包含连接开始时的清单指纹及当前阅读进度；源码清单变化会提示不能直接比较。使用完整审阅后的报告比较工具总耗时、调用间隔、重复行和失败重试，同时核对覆盖报告和抽样源码关系。工具耗时降低不自动证明整体 AI 审阅更快，也不能以减少分析深度换取“完成”。

旧进程和已经过去的耗时不会补记。正在阅读时保留原连接；一批成果成功提交后再更新并重连新版 MCP，继续同一地图。`blueprint_guide` 不带地图参数，因此未计入某张地图的统计；入参校验失败、打不开地图和初始化失败也不在采集范围内。直接 CLI 读取和画布 HTTP 浏览不计作 MCP 阅读调用。旧成果仍可查看，未记录的历史显示为空，不伪造零耗时。

在源码仓库中运行 `python scripts/benchmark_metrics.py --output work/new-metrics-benchmark`，可复测记录统计增加的本地开销。输出目录必须是新目录；脚本复制四份源码、使用独立地图，交替执行相同的有统计/无统计读取，并保存测量结果及样例报告。它不会启动模型或推进语义审阅。已有测量范围和结果记录在源码仓库的 VALIDATION.md。

## 看清覆盖范围

分析任务领取时会收到模块阅读包，按页组合真实源码、结构、关联线索、已有结论和待办；无需在画布里额外启动一次解析。外部 MCP Agent 和本机后台阅读器使用同一接口。提问模式也可按节点或文件取包，不领取任务。包已送达只表示材料可读，完成状态仍由 Agent 的实际审阅与提交决定。

“覆盖”分成四类，可搜索并打开已记录场景或继续分析缺口：

| 分类 | 表示什么 |
| --- | --- |
| 文件阅读 | 纳入范围的文件是否有完整阅读记录，排除文件另行标识 |
| 已整理场景 | 已记录的共同入口、调用上下文及关系，可跳转流程画布 |
| 未纳入场景 | 图谱中尚未属于已记录场景的函数，包括已读函数 |
| 待确认关系 | 未解析、推断或源码已经过期的关系及具体原因 |

“源码全部已读”不等于所有流程、分支、异常或运行路径已覆盖。场景统计依赖已保存记录，不会自动创造场景。新增源码或过期依据显示需要复核。

## 更新和保留身份

“检查代码变化”先展示变化分类和复核范围，登记新版本后可在 Agent 面板继续阅读。Python 函数体修改根据已记录的函数依据和调用者收窄影响；能保守判定的注释/格式变化保持局部复核。接口、构建与无法判断的变化扩大复核；其他语言还没有完整语义差异解析。

唯一且内容相同的文件移动会自动关联旧身份。移动时同时修改了内容，可在预览里选择旧文件与新文件，点“按重命名关联”，再登记版本。相同内容的多份复制不会擅自关联。已匹配的文件和函数保留身份、布局及颜色，路径变化相关的引用仍会复核。

## 对比和恢复历史

“历史”选择归档版本后，展示历史与当前的文件、节点、连线、数据身份变化，以及可用的源码文本差异。新版本保存时按实际指纹缓存可读取文本；早期版本或超过读取上限的文件可能没有文本，会明确提示。

“以此版本恢复图谱”先归档当前图谱，再把所选分析结果恢复为一个新版本。仓库源码保持现状，阅读布局保留；与现有源码不一致的依据重新排队核对。可再次选择恢复前的归档撤回这次操作。正在执行的阅读请求须先暂停；比较后源码或图谱又变动时，需重新比较。

归档发生在源码快照更新和历史恢复之前，不是每次节点点击或每个分析批次都自动创建一个可选历史版本。
# 本地结构解析

左侧“本地结构 → 解析结构”会分批提取整个清单内的代码声明，并复用未改动文件。进入“仓库结构”，展开文件、命名空间、类和函数；节点详情显示声明、参数及源码位置。生成节点标为“结构已解析”，审阅进度独立保留。语法错误和缺少的解析器会列在“解析范围与缺口”中。

Agent 领取任务时自动获得结构索引，可用 `blueprint_index` 分页查看，再在提交准备中引用 `symbol_id`。所有 MCP 宿主共享这条路径，不需要 Agent 手工重写每个节点的结构字段。首次初始化只预解析前 100 个文件，更大的仓库用画布按钮或 `python scripts/run.py index 地图目录` 继续全部结构解析。
