# 仓库图谱交换格式

版本：0.1 · 2026-09-12

本文定义首个可读写、可校验的 JSON 交换格式。对应实现为 [图谱与协议校验](codemap/core.py)，人工整理的示例见 [sample-map.json](examples/sample-map.json)。它是格式与任务协议的参考实现，尚无自动代码分析器。

## 1. 保存与交换

一个地图工程对应一个仓库身份和当前分析快照。当前参考实现使用一个 JSON 图谱文档作为交换文件，SQLite 事务保存图谱、任务及批次回执；用户布局单独保存。

大型仓库仍以分片存储、按需加载为目标。当前存储每次读写完整图谱文档，适合协议验证和小型样本；不能据此宣称已支持 UE 规模。

顶层字段如下，集合中的记录均有稳定 `id`：

| 字段 | 内容 |
| --- | --- |
| `format_version` | 当前为 `0.1` |
| `project` | 工程身份、源码快照、修订号、全仓库模式和执行控制 |
| `sources` | 仓库文件清单、内容指纹、阅读状态和符号登记状态 |
| `entities` | 源码实体、节点种类、角色、解释、分析进度和依据 |
| `memberships` | 文件位置、语义作用域、架构分组之间的归属 |
| `contexts` | 局部分析上下文与具体调用位置 |
| `ports` | 某实体在某上下文中的输入输出端口 |
| `flows` | 数据身份、产生位置、值版本、派生来源和稳定数据编号 |
| `relations` | 源码实体关系或具体数据端口之间的关系 |
| `evidence` | 支持结论的文件、内容指纹、位置与说明 |
| `tasks` | 全仓库必需工作、依赖、执行状态和恢复信息 |
| `receipts` | 已提交批次的内容摘要与提交版本 |

角色、颜色和布局不作为源码实体身份的一部分。文件位置、种类、名字相同不代表它们是同一个实体；名称变化也不自动生成全新的身份。

## 2. 工程与范围

`project` 必需字段：

| 字段 | 值与规则 |
| --- | --- |
| `id`、`name` | 稳定工程标识与显示名称 |
| `snapshot_id` | 本次分析的源码与配置快照标识 |
| `revision` | 从 0 开始的整数；图谱或任务控制提交后递增 |
| `mode` | 当前参考实现为 `whole_repository`；局部问题作为同一工程内的任务 |
| `inventory_complete` | 是否已经登记完整范围；由实际范围发现过程提供 |
| `execution` | `active` 或 `paused`，控制是否允许领取下一批 |

`sources` 必需字段为 `id`、`path`、`category`、`sha256`、`included`、`read_state`、`symbols_complete`。排除或读取失败时还必须有 `reason`。

- `path` 是使用 `/` 的仓库相对路径；禁止绝对路径、向上跳转和解析到仓库之外的链接。
- `category` 为 `source`、`build`、`documentation` 或 `resource`。
- `read_state` 为 `unread`、`read` 或 `failed`。
- `symbols_complete` 表示该文件的符号清单是否已经登记完整，不等于函数语义已核对。
- `included` 为 false 的内容必须在报告中列出，不能计入已读源码。

普通分析批次可以更新阅读状态，不能改变既有源码指纹、路径和范围。新增、删除或变化的源码通过 `blueprint_update` 预览并迁移到新快照，旧图谱归档在原工程数据库中。同一路径的来源身份保留；移动文件暂按删除和新增处理。

## 3. 实体种类和状态

`entities` 每项保存：

```json
{
  "id": "fn:move",
  "kind": "method",
  "name": "Move",
  "language": "cpp",
  "roles": ["entry"],
  "source_ids": ["src:motion"],
  "summary": "根据输入计算并写回位置",
  "analysis": "partial",
  "freshness": "current",
  "evidence_ids": ["ev:move"]
}
```

此片段展示字段，不是独立完整图谱。具体种类见 [种类目录](codemap/node_kinds.json) 与 [节点视觉规范](NODE_DESIGN.md)。

`analysis` 为 `located`、`partial` 或 `reviewed`；`freshness` 为 `current` 或 `stale`。节点解释有源码支持才可以设为 `reviewed`。唯一的空清单例外是完整目录扫描确认无任何文件时的仓库/目录概况，可以凭清单核对结果记录为空，不虚构源码依据。函数和方法还必须提供 `details` 中的 `inputs`、`outputs`、`calls`、`reads`、`writes`、`conditions` 数组；空数组明确表示当前已核对范围中无此项。

角色与种类相互独立。`entry` 标记入口，不把方法改成另一种实体。测试、工具、生成内容与第三方来源也使用角色标签。

源码实体可以引用多个源文件和证据。普通批次保持既有种类；`unknown` 可以在识别后改成具体种类。其他种类变化需要明确的身份迁移，避免覆盖另一个实体。

## 4. 归组、上下文与端口

实体可带可选的 `structure` 声明定位元数据，由本地解析器产生：包含 `source_id`、`sha256`、正整数 `start_line` / `end_line`、字符串 `signature` 和参数对象数组 `parameters`（每项含字符串 `name`、`type`），并保留解析器版本、索引身份及语法状态。此元数据不是语义证据，不代替 `evidence_ids` 或函数的六项 `details`。文件指纹改变后，画布隐藏旧定位，等待当前版本的解析结果。

尚未审阅的结构保存在独立 SQLite 缓存中，按文件指纹、路径和解析器版本复用；不改变主图谱修订号、任务租约、`read_state` 或 `symbols_complete`。画布将它们投影为 `analysis=located` 的节点，标注“结构已解析”，覆盖报告仍以已提交的审阅记录为准。通过 `blueprint_prepare` 引用 `symbol_id` 时，选中的实体与必要父级才进入标准提交；未解释的父级仍是 `located`。MCP `blueprint_query` 和 JSON 导出保持已提交记录的语义。

`memberships` 使用 `parent_id`、`child_id`、`axis`。`axis` 为 `physical`、`semantic` 或 `architecture`。同一实体可以参与多种归组；各轴内不允许包含关系形成环。

归组不是调用或数据传递。拓扑画布按所选归属轴生成折叠节点与跨组端口，汇总线保留全部原关系；类型、方向、数据身份或调用上下文不同的关系不会合并。内部关系保留在组内，展开可定位，不能因为同一目录而生成数据关系。投影结果属于阅读视图，不写成新的源码事实。多重归属无法确定唯一代表时保留原实体并提示。

`contexts` 记录 `id`、`parent_id`、`caller_id`、`callee_id` 和具体调用位置的 `callsite_evidence_id`。局部上下文的根没有调用目标。不同调用位置创建不同上下文；递归的显示范围由分析任务控制，不能无限展开上下文。

流程阅读按根分析上下文组织处理场景，调用上下文展开为子流程。属于同一次处理的多个输入保留在一个流程中；独立事件或请求分别建立根上下文。顶层 `flows` 仍表示独立的数据身份，不表示整个业务流程。画布生成的共同入口与灰色归属线只保存在视图投影中，不作为 `entities` 或 `relations` 提交。

`ports` 必需字段为 `id`、`entity_id`、`context_id`、`name`、`direction`、`channel`、`type`。

- `direction` 为 `in` 或 `out`。
- `channel` 为 `data` 或 `call`。
- 同一个函数在两个调用位置的端口使用不同身份，并关联同一个源码实体。
- 数据跨调用边界时，只能连接本次调用上下文与它的父上下文；进入或离开的子端口必须属于本次调用的目标。

这些检查能识别结构上的调用上下文混接。对象别名、动态派发和参数的真实依赖仍须依据源码核对。

## 5. 数据与关系

`flows` 必需字段：`id`、`name`、`producer_port_id`、`value_version`、`derived_from`、`color_key`、`evidence_ids`。

产生位置必须是数据输出端口。原值传递沿用流身份；计算产生新流并记录输入来源。`derived_from` 不允许成环；循环或反复写入通过值版本表示，基础源码图中的控制回边可以保留。

`color_key` 是保留兼容的数据编号字段（如 D12），同一工程内每个流独立分配，已有编号保持稳定。它不再决定画布的主颜色。画布按根分析上下文分配任务颜色，子调用继承根任务颜色；多个输入和派生结果仍保留各自的 `flow_id`、版本与来源。任务颜色映射 `taskColors` 和任务选择 `taskFocus` 保存在阅读视图中，不写成源码事实。图例用途取自入口实体已保存的职责摘要，缺少说明时显示待核对。

`relations` 保存 `id`、`kind`、`from_id`、`to_id`、`context_id`、`basis`、`freshness`、`evidence_ids`。

| 关系 | 端点与附加信息 |
| --- | --- |
| `data`、`read`、`write` | 从数据输出端口到输入端口，必须有 `flow_id` |
| `call` | 从调用者实体到被调用者实体；与具体上下文中的调用目标一致 |
| `control` | 同一分析上下文中的实体到实体，记录有源码依据的执行转移或控制回边；不带数据流标识 |
| `inherit`、`implements`、`reference`、`event` | 从实体到实体；不带数据流标识 |

`basis` 为 `source`、`candidate` 或 `unresolved`。源码支持的关系必须有证据；候选和未解析关系必须有 `reason`。`freshness` 单独表示当前依据是否需要重新核对。

数据流向、调用和成员归属都有独立记录。用参数调用某个函数，不能自动推导其所有返回值都依赖这个参数。

`control` 必须由实际分支、顺序或循环转移的证据建立，不从调用或数据回传自动生成。运算实体可用于表示有源码位置的控制步骤。静态调用环只表示可能递归；控制环按上下文分别分析，执行与否仍取决于运行条件。候选或过期关系不用于确认环；没有控制关系表示尚未记录控制流，不能据此断言代码没有循环。现有 C++ 阅读样本尚未补齐控制流事实。

## 6. 源码依据与完成报告

证据保存源文件标识、`sha256`、从 1 开始的 `start_line` 和 `end_line`，以及支持结论的 `note`。校验可选读取本地源码，对比完整内容指纹并检查行号范围。

增量更新扩展允许 `evidence`、`contexts`、`ports`、`flows` 携带可选 `freshness`，省略时视为 `current`。旧证据被标为 `stale` 时保留原哈希和范围，可与新源码不同；当前结论不得引用过期证据。重读后必须提交匹配新内容的依据与已核对记录，不能只修改哈希伪装完成。过期上下文、端口或数据身份也阻止完成报告。任务的 `review_ids` 记录本轮必需核对的已有记录；顶层可选 `last_update` 保存变化清单、影响范围、计划标识及归档修订号。

完成报告根据已登记清单、节点深度、关系状态和必需任务计算：

- `inventory_incomplete`：完整范围尚未登记。
- `partial`：仍有未读或未达到深度的内容、过期实体或必需待办。
- `coverage_with_unresolved_relations`：阅读与深度已覆盖，但仍有候选、未解析或过期关系。
- `complete`：已登记范围的各项检查均满足要求。

这是结构与状态的判定。它不能证明范围清单未漏文件，也不能证明 AI 的语义结论正确；真实范围扫描、符号发现和源码核对是后续分析链路的责任。参考示例保留诊断与测试待办，状态为 `partial`。

## 7. 已实现和待实现

已实现记录种类、引用、端口方向、调用上下文、证据指纹、归组及派生环检查，提供 JSON 示例和只读校验入口。任务与批次提交规则见 [任务协议](TASK_PROTOCOL.md)。

已补充：完整目录扫描、源码分段读取与搜索、命令及 MCP 任务桥接、提交前的当前源码核对、真实图谱画布、拓扑折叠与端口聚合、主题布局保存。固定五文件 C++ 样本已通过完整成果提交与源码核对，插件已通过 Codex 工具连接与提交验证。

已补充增量快照迁移、变更分类、局部重读、移动身份关联、受控记录退役、历史图谱与源码文本对比、图谱恢复及阅读设置保护。画布请求保存于独立表并连接实际 Codex 阅读器；文件阅读与场景覆盖分别统计。待实现：通用语言语义解析、图谱分片和复杂多语言/大型仓库验收。扫描的文件语言分类不代表完整语义支持。

## 8. 仓库扫描扩展

扫描器在 v0.1 图谱上增加可选的 `inventory` 信息，包含 `policy`、`exclusion_patterns`、`excluded_paths`、`gaps` 和 `empty_directories`。无法枚举或获取指纹的路径进入 `gaps`，不会伪造来源哈希；出现缺口或扫描期间目录变化时，`inventory_complete=false` 并保留阻碍任务。

工程保存本地 `source_root`；来源增加 `language`、`bytes`、`encoding`。被显式排除的目录作为边界记录，非文本资源可作为来源登记但不进入代码阅读队列；不可读源码仍是必需待办。排除来源专属的实体不阻止纳入范围的完成判断，排除清单保持可见。

源码行号按解码后的文本计算，空文本视为一行空逻辑行，允许用第 1 行与文件指纹证明其为空；读取结果同时给出 `empty_file=true`。读取空文件本身仍不自动标记阅读任务完成。

首次文件/目录 ID 由相对路径生成；快照迁移匹配文件移动后保留已有来源和文件 ID，不能根据新路径重新生成身份。唯一同内容移动自动匹配，编辑后的移动需明确的一对一关联。目录本身尚无独立移动身份推断。画布以实体和调用上下文组合生成显示实例，分别展示同一函数的不同调用。变化预览只读；应用更新会在一次事务中归档旧图谱、迁移新清单并登记受影响任务。被删除来源专属实体及无法保留的依赖记录从活动图谱移除，旧记录保留在归档中。用户阅读设置位于独立表，不参与该迁移。

历史恢复可添加 `last_restore`，记录历史版本、恢复前版本、新修订号、恢复标识及时间，不改变核心 v0.1 记录结构。源码文本按内容指纹保存在 `source_texts` 表；它是对比缓存，不是完成阅读的证据。`reader_requests` 和 `reader_worker` 独立记录画布请求及实际执行连接，不写入普通分析批次。
