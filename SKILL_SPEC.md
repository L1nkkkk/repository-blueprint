# 仓库梳理 Skill 工作规范草案

版本：0.2 · 2026-09-12

本文细化 AI 阅读仓库、整理图谱和持续补充分析的工作约定。它是供评审和实现使用的规格草案；其中的输出字段和流程尚需通过真实代码样本验证。

对应的操作流程见 [完整使用流程](USER_FLOW.md)，组件与验收边界见 [首版交付规格](V1_SCOPE.md)。Skill 规定工作方法；持续执行、任务接收与恢复状态由实际执行器和任务管理组件提供。

可执行版本见 [repository-blueprint Skill](skills/repository-blueprint/SKILL.md)，随个人插件安装。它使用 MCP 扫描、读取、任务与提交工具，供当前 AI 会话逐批推进，命令入口继续可用。画布另有显式请求连接本机 Codex，在独立进程中逐批分析并持久保存结果、暂停与续接状态。安装与继续已有工程见 [调用指南](INSTALL.md)。

## 1. Skill 的交付目标

将用户指定范围内的代码整理成可阅读、可追溯、可继续补全的仓库地图工程。AI 使用现有文件读取与搜索能力开展阅读，按需要调用符号查询、语言分析等辅助工具。

全仓库梳理是默认主模式：用户指定仓库后，AI 自动识别项目组织与入口集合，遍历仓库代码、逐模块阅读、整理跨模块关系，最终交付完整范围的仓库地图工程。用户无需预先提出具体函数或数据问题，也无需逐个点击节点才能推进分析。

局部流程分析、具体数据追踪和节点深入拆解是辅助模式。用户明确限定范围时遵循该范围；在全仓库任务中临时关注某个节点，只调整分析优先级与展示范围，保留全仓库任务及剩余队列。

每次分析围绕四类成果展开：图谱内容、解释、源码依据、进度与边界。已有且仍有效的成果可以直接复用。

用户指定的仓库、问题和范围决定任务边界。分批输出用于保存进展；若用户要求梳理整个仓库，模块队列中尚有必需工作时，阶段性输出应继续标为部分成果。

## 2. 支持的使用意图

| 用户意图 | AI 的主要工作 | 本次应交付的变化 |
| --- | --- | --- |
| 全仓库梳理（主模式） | 建立完整覆盖清单，逐模块阅读至函数级，关联入口、调用与数据读写，核对跨模块关系 | 仓库全景、完整层级地图、模块说明、入口与流程图集、数据关系及覆盖报告 |
| 浏览全仓库阶段性成果 | 展示全仓库任务已形成的图谱与尚未完成的范围 | 当前总览、模块进度及后续队列 |
| 梳理一段流程 | 从指定入口沿相关调用、条件和副作用展开 | 流程子图、输入输出、关键分支及终点或边界 |
| 追踪具体数据 | 从指定参数、字段或表达式追踪传递与派生关系 | 数据路径、转换说明、读写位置和消费位置 |
| 继续拆解节点 | 阅读该节点关联的内部实现及必要依赖 | 新增子节点、内部关系、外部端口到内部位置的对应 |
| 更新已有分析 | 比较代码与已有证据，核对受影响的结论 | 局部更新、失效标记、更新说明 |

这些意图共享一个主 Skill 和一套结果格式。较长的语言说明按需要加载，避免每次阅读全部语言参考。

## 3. 输入与分析范围

全仓库模式的最低输入是可访问的仓库。局部模式还使用用户明确指定的问题或范围。用户已经给出的入口、数据、模块和偏好应直接沿用。

分析开始时记录：仓库标识、实际读取的源码版本、用户问题、关注范围、入口或数据起点，以及已知的构建目标或配置。Git 提交号与已读取文件的内容指纹共同描述版本；没有 Git 时仍记录文件指纹。

构建配置未知时明确记录未知。涉及条件编译的结论应说明所依据的条件，必要时展示候选分支。

全仓库模式先建立覆盖清单和模块概览，再按入口与依赖关系安排阅读顺序，持续处理尚未完成的范围。入口可达性用于安排优先级，未从入口到达的模块、工具、测试和独立代码仍纳入覆盖清单。

清单登记仓库内的源码、构建与配置文件、说明文档、测试、脚本、生成代码、随仓库提供的第三方代码及资源等类别，并说明各自采用的处理方式。对可访问源码默认逐项安排分析；资源与不可读内容保留依赖或边界说明。任何排除项、未支持内容或读取失败都明确列出，不计为已经完成的源码分析。仓库过大不能成为静默省略模块的理由。

## 4. 每次应交付的内容

全仓库模式以一份统一的仓库地图工程为最终交付，其中包含以下相互关联的视图。每批分析更新这份工程，局部子图是工程的组成部分。

| 全仓库成果 | 具体内容 |
| --- | --- |
| 仓库全景图 | 子系统、模块、插件、工具和测试等分区，及其职责与关系 |
| 完整层级地图 | 文件夹、文件、语义作用域、类型和函数的索引与对应；未分析实体保留状态 |
| 模块内部说明与子图 | 各模块的职责、核心数据、函数输入输出、直接调用和成员读写 |
| 跨模块关系图 | 依赖、调用、共享状态、事件与消息等关系及依据或候选说明 |
| 入口与流程图集 | 已发现的程序入口、框架事件入口、任务入口及导出接口等对应的流程 |
| 数据关系图集 | 参数、字段与共享对象的来源、变换、读取、写入和消费位置 |
| 覆盖与缺口报告 | 全部登记范围的阅读进度、分析进度、已核对关系、未解决项及排除项 |

每一批分析以及局部辅助模式，应维护以下内容：

| 成果 | 最低内容 | 在画布中的用途 |
| --- | --- | --- |
| 架构总览 | 已识别的模块、职责、入口和主要关系 | 决定从哪里开始阅读 |
| 流程与数据子图 | 节点、端口、关系类型、流向、条件及分析边界 | 展开流程和追踪数据 |
| 节点解释 | 一句话职责；输入、处理、输出或副作用 | 快速阅读与选中后的详情 |
| 源码依据 | 相关声明、调用、传参、赋值或读写位置 | 核对图上的结论并跳转源码 |
| 进度与待办 | 已读范围、已回答部分、未分析部分、未解决问题和下一步 | 判断覆盖情况并接续工作 |
| 本轮更新说明 | 新增、修正或失效的结论及其影响范围 | 理解图谱发生了什么变化 |

进度报告使用可核对的范围。只有已知统计对象及总量时才使用比例；阅读过若干文件不等于同等比例的语义分析已完成。

## 5. 建议的阅读与整理过程

### 全仓库任务的推进方式

1. 建立仓库快照与覆盖清单，登记代码分区、语言、配置和待处理内容。
2. 生成全局结构骨架，定位类型与函数，并识别多个入口和公开接口。
3. 建立覆盖所有源码单元的模块队列，按依赖和用户关注点安排顺序。
4. 分批阅读各模块至函数级，整理职责、参数、返回值、调用、字段读写及必要的条件与副作用。
5. 将模块之间尚未核对的关系放入跨模块队列，持续回查关联代码与配置。
6. 汇总全景、模块子图、入口流程和数据关系，核对是否存在遗漏、混接或过期证据。
7. 按覆盖清单检查各项完成程度，并单独报告无法解析或仍需核对的内容。

分批单位可以按模块、文件或函数大小调整。每批完成后保存图谱、证据、待办和进度，再继续下一批；遇到执行中断或必要信息缺失时保存恢复点。恢复后读取已有成果和剩余队列，核对版本变化后接续工作。

范围与深度分别记录。全仓库模式默认要求对全部范围内源码建立至函数级的结构、职责、输入输出、直接调用和成员读写记录，并汇总数据传递与派生关系。无法读取、尚未分析或无法确认的部分保持可见，单纯登记名称不算完成这些分析项。

以下阅读与核对步骤在各批次中复用：

### 定位范围并读取已有成果

读取已有图谱、进度和源码版本信息，确认当前模式、全局队列与用户关注点。核对已有证据是否仍然有效，再决定复用或重新阅读。

### 建立代码骨架

从目录、项目说明、构建信息和实际声明中识别文件与代码符号。候选入口、模块职责和已确认的声明分别记录。目录名与函数名可以帮助导航，职责解释应结合实际实现。

### 沿用户问题阅读

阅读入口实现、直接相关调用、数据读写及必要的类型定义。遇到分支、提前返回、异常、回调或外部接口时，判断它们是否影响当前问题，并记录相关条件或边界。

对调用记录调用位置、参数与返回值的对应。同一函数的不同调用位置保留必要的上下文，避免将不同调用的输入和返回结果混接。

### 整理数据的传递与变化

区分传递、复制、字段映射、计算、合并和对象状态修改。追踪对象状态时区分读取前的值与写入后的值；指针、引用或共享对象的指向不明确时，保留候选关系和限制。

调用关系与数据关系分别记录。消费了一个参数，不代表所有返回值都依赖该参数；每条数据依赖都需要对应的代码依据或明确的推断说明。

### 核对关系并提交本轮成果

检查图谱引用、端口对应、源码锚点和解释的一致性，将可复用的内容保存下来。沿着当前问题从起点回读一次，确认必要的分支、写入点和未解析边界均有交代。

完成后更新进度和待办。下一轮从已有记录继续，必要时检查变化的源码及受影响结论。

## 6. 图谱需要表达的逻辑对象

下面规定对象职责，首个可校验的序列化格式见 [图谱格式](GRAPH_FORMAT.md)。节点种类见 [节点视觉规范](NODE_DESIGN.md)，角色标签、分析进度和源码时效分别保存。

| 对象 | 必需表达的信息 |
| --- | --- |
| 工程记录 | 仓库、源码版本、分析配置、全仓库或局部模式、用户范围、图谱版本 |
| 代码实体 | 稳定标识、类型、名称、语言、源码关联、职责与分析状态 |
| 归组关系 | 文件归属、语义归属或架构分组，及具体成员 |
| 输入输出端口 | 所属实体、方向、参数或字段路径、类型、相关数据标识 |
| 代码关系 | 调用、引用、继承等明确类型，以及相关上下文和依据 |
| 数据关系 | 起终端口、传递或变换语义、条件、调用上下文和依据 |
| 数据流 | 稳定标识、名称、类型、来源及派生关系 |
| 证据 | 文件、源码指纹、位置、相关片段或工具结果、支持的具体结论 |
| 覆盖清单与分析进度 | 全部登记范围、各单元的目标深度与当前状态、模块队列、跨模块核对队列、恢复点及未解决问题 |
| 阅读视图 | 节点位置、锁定状态、展开状态、颜色映射、书签与用户注释 |

代码实体可以关联多个源码位置，例如声明与实现。文件位置与语义归属分别表达。

文件夹、文件和类的折叠视图由内部实体及关系归组得到。折叠端口应能映射回内部端口；归组不能制造原本不存在的数据路径。

## 7. 数据流身份与颜色

- 有依据的原值传递、复制或名称映射沿用数据流标识；同源分支保持一致。
- 计算、合并或内容变化产生新的数据流标识，并记录其输入来源。
- 追踪某个输入的去向时可以沿派生关系继续；数据名称、版本与源码依据说明转换。同一任务内的输入和派生结果共用任务颜色，颜色由根分析上下文决定。
- 同名字段、不同调用位置或不同对象实例，不能仅凭名称合并成同一份数据。
- 源码中的写入产生新的值版本，保留前后关系。无法判断对象是否相同时，应明确记录不确定性。
- 任务颜色映射与用户布局持久保存在阅读设置中，图例用途来自入口的职责说明。稳定标识的匹配不明确时，保留待核对的对应关系。

## 8. 分析状态与证据状态

三类状态分别表达，避免将阅读进度、结论可靠性和源码时效混为一体。

| 维度 | 建议状态 | 含义 |
| --- | --- | --- |
| 分析进度 | 已定位未读、部分分析、指定范围已核对 | 当前问题涉及的内容阅读到了什么程度 |
| 结论依据 | 源码支持、推断或候选、尚未解析 | 这条关系依据什么成立，哪里仍然不确定 |
| 源码时效 | 当前、待重新核对 | 证据是否匹配当前源码与分析配置 |

“源码支持”描述特定源码版本与配置下的静态关系。若以后接入运行记录，应另外标注其实际观察范围。

对需要多个位置共同支持的结论，保存多个证据引用。注释和文档可解释设计意图，关键传递关系同时核对相关实现。

## 9. 怎样判断梳理完成

### 全仓库模式

- 覆盖清单对应当前仓库快照，所有源码分区都已登记，排除项与不可访问内容有明确说明。
- 范围内的源码单元达到规定的阅读与记录深度；模块职责、类型与函数、直接调用、输入输出和成员读写均有对应成果。
- 跨模块关系已经核对，或明确标出无法确认的连接及其影响，保留仍需处理的工作。
- 已发现的各类入口、公开接口、独立模块和未从入口到达的代码都有对应记录。
- 全景图、模块子图、流程图和数据关系使用一致的实体标识，源码证据仍有效。
- 完成报告分别说明结构覆盖、源码阅读、语义梳理和关系核对的程度。

存在未读的范围内源码、未达到目标深度的实体或仍可继续处理的必需队列项时，整个任务继续标为未完成。若阅读已经覆盖全仓库但存在无法解析的关系，应准确报告“全仓库阅读已覆盖，仍有未解析项”，并列出内容与影响。将问题移到待办、失败列表或未知节点，不会自动使相应分析项完成。

### 局部辅助模式

范围内的结论应满足以下条件：

1. 起点明确：入口、调用上下文或数据起点能定位，用户的问题及分析范围已记录。
2. 处理过程可解释：关键节点的职责、输入、处理、输出或副作用具有依据。
3. 路径可衔接：相关传参、返回值、字段映射和派生关系能接起来；上下文不会混接。
4. 条件与终点清楚：影响结论的分支、写入、消费位置、结束条件及外部边界有交代。
5. 核心问题已回答：用户关心的“从哪里来、怎么变化、去了哪里”在约定范围内得到回答。
6. 图与源码一致：必要的源码引用仍有效，图谱、解释及分析状态相互一致。

“本轮已经交付”和“请求已经完成”分别报告。如果关键去向仍未查清，可以交付带边界的部分结果，继续保留对应待办；未知关系应具有可见的说明和下一步。

图谱格式与引用校验用于发现缺失实体、错误端口、无效位置等问题。语义结论还需要回读实现或使用适当的分析工具核对。

## 10. 画布与 AI 的交互约定

画布提供全仓库梳理的启动、暂停、继续和进度查看入口。任务与进度管理模块负责保存全局队列、图谱版本、已完成批次和恢复点。使用者可以在分析推进时浏览已经形成的总览和子图。

这些操作需要真实执行连接。请求发送、接收确认、执行中和已完成分别记录；连接不可用时提供可复制的上下文请求，并保持等待状态。Skill 与恢复点本身不代表宿主停止后仍会持续运行。

“展开已有内容”由画布读取已保存的子图完成。“继续分析这里”向 AI 提供：工程与图谱版本、目标节点或数据标识、用户问题、希望分析的范围和必要上下文。

全仓库模式中的节点展开主要用于浏览已分析内容；对未分析节点请求深入，可以提高相关任务的优先级，全局剩余模块仍继续保留在队列中。

AI 返回本轮新增或修正的实体、关系、证据、状态和更新说明。提交更新时检查基于的图谱版本；已有内容发生变化时，核对差异后再合并。

已有 v0.1 [任务与批次协议](TASK_PROTOCOL.md) 实现，提供执行身份、版本检查、重复提交识别和事务保存。正式 Skill 已连接 MCP 和命令入口；画布本地 Codex 适配器也已完成真实提问、阅读与提交验证，支持独立后台执行。固定样本重放、实际模型分析与复杂语言验收分别记录，不互相替代。

用户拖动位置、锁定节点、修改显示名称或添加注释保存在阅读视图中。分析内容更新时保留这些阅读设置；源码实体消失或身份无法匹配时，提示关联需要重新确认。

## 11. 交付示例

下面是用于讨论的虚构 C++ 风格片段，省略类型定义，不代表 UE 实际实现：

```cpp
void Move(const Input& input, float dt, State& state) {
    auto velocity = ComputeVelocity(input.direction, input.speed);
    auto next = state.position + velocity * dt;
    state.position = next;
    PublishPosition(next);
}
```

针对“输入如何影响最终位置”，本轮应整理出：

- input.direction 与 input.speed 分别传入 ComputeVelocity 的对应参数；返回值接到 velocity。其内部计算和输入依赖应继续阅读实现后确认。
- velocity、dt 和读取到的 state.position 参与 next 的计算，记录相应派生关系。
- next 写入 state.position，并传给 PublishPosition。这两个传递分支沿用 next 的数据标识。
- PublishPosition 的实现尚未阅读时，将其标为待继续分析的边界。具体消费位置以实现为依据。
- 每条关系带对应代码位置；进度中列出已核对的部分，以及 ComputeVelocity、PublishPosition 尚需阅读的内容。

只有补齐用户范围内需要的内部计算、依赖和去向，并核对相关条件后，才将该请求标为已完成。

## 12. 下一步的最小验证范围

先对人工能够核对的小型、多模块 C++ 仓库执行一次全仓库梳理，覆盖入口路径、未被入口调用的模块、测试与构建脚本，以及跨文件传参、返回值、字段写入、条件分支、同一函数的不同调用位置和外部边界。检查是否所有范围内代码都进入覆盖清单、是否达到规定深度，以及中断后能否恢复整个仓库的剩余任务。

同时验证局部追踪能否复用全局成果、图谱能否正确衔接、续写时能否保留布局，以及完成状态是否准确。随后扩大到 UE 仓库和其他目标语言样本，按真实问题补充语言参考和辅助脚本。
